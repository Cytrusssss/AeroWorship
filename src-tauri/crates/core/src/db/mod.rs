//! SQLite storage: migrations, queries and the FTS index (PRD §6.13).
//!
//! Empty until the item that introduces the schema. Nothing here may reach for
//! an application path directly — paths are resolved by the shell through the
//! Tauri path API and handed in as arguments.
