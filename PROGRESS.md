# PROGRESS — AeroWorship

Status pengerjaan seluruh item yang diturunkan dari [`docs/PRD.md`](docs/PRD.md).
Dikelola oleh agent `project-lead`. **Jangan diedit manual saat siklus agent sedang berjalan.**

| Field | Nilai |
| --- | --- |
| Dibuat | 2026-08-07 |
| Terakhir diperbarui | 2026-08-08 |
| Sumber requirement | `docs/PRD.md` v1.0 (MVP) |
| Total item | 7 setup + 67 functional requirement + 10 release gate = **84** |
| Selesai | 2 (SETUP-01, SETUP-02) |

## Legenda

| Status | Arti |
| --- | --- |
| `todo` | Belum dimulai. |
| `in-progress` | Sudah didelegasikan ke implementer, belum lulus verifikasi. |
| `blocked` | Terhenti. Wajib disertai alasan di kolom Catatan. Dua kegagalan berturut-turut pada satu item otomatis menjadikannya `blocked`. |
| `done` | Acceptance criteria terpenuhi, `tester` lulus, `security-auditor` nol temuan Critical. Tidak ada jalan lain menuju status ini. |

**Prioritas** mengikuti PRD §4: `M` = Must (blocker MVP) · `S` = Should · `C` = Could (kandidat pasca-MVP).

**Kolom Keputusan** menunjuk entri di [`decisions.md`](decisions.md) yang mengikat item tersebut.

Detail tiap item — teks requirement, acceptance criteria, dan user story yang ditrace — ada di `docs/PRD.md`. Cari dengan ID-nya.

---

## Fase 0 — Scaffolding

Belum ada satu baris kode pun di repo ini. Item berikut mendahului seluruh FR.

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| SETUP-01 | Inisialisasi shell Tauri 2.x + Cargo workspace `src-tauri/`, sesuai layout PRD §6.13 | M | `done` | 2026-08-08 | [ADR-0001](decisions.md#adr-0001) · [0008](decisions.md#adr-0008) · [0009](decisions.md#adr-0009) · [0010](decisions.md#adr-0010) · [0012](decisions.md#adr-0012) · [0013](decisions.md#adr-0013) · [0014](decisions.md#adr-0014) | 3 siklus. Tester LULUS, auditor 0 Critical. Workspace 2 crate; `panic="unwind"`; capabilities = `core:event:default` saja; `dynamic-acl` mati. **Terbawa ke SETUP-02:** aplikasi belum pernah dijalankan — butuh `dist/`. **Terbuka (Warning, ditunda sadar):** [ADR-0011](decisions.md#adr-0011) `webviewInstallMode`, [ADR-0014](decisions.md#adr-0014) sisa jalur ACL runtime. |
| SETUP-02 | Vite dua entry point: `index.html` (Control Panel, bundle penuh) dan `output.html` (Projector, bundle minimal) | M | `done` | 2026-08-08 | [ADR-0015](decisions.md#adr-0015) · [0016](decisions.md#adr-0016) · [0017](decisions.md#adr-0017) · [0018](decisions.md#adr-0018) · [0019](decisions.md#adr-0019) | 2 siklus. Tester LULUS, auditor 0 Critical / 0 Warning. Pemisahan bundle terbukti dari array `sources` sourcemap: `main-*.js` hanya memuat `src/main/**`, `output-*.js` hanya `src/output/**`; per window 61,5 KB (60,1 KB di antaranya runtime Vue bersama). GATE-G5 pertama: installer ~1,39 MB. Utang verifikasi runtime dipindahkan ke SETUP-07 agar punya pemilik, bukan terus dititipkan. |
| SETUP-03 | Toolchain kualitas: `clippy -D warnings`, `rustfmt`, ESLint, Prettier, `vue-tsc`, Vitest | M | `done` | 2026-08-09 | [ADR-0015](decisions.md#adr-0015) · [0018](decisions.md#adr-0018) · [0020](decisions.md#adr-0020) · [0021](decisions.md#adr-0021) · [0022](decisions.md#adr-0022) | **7 siklus.** Tester LULUS (145 test, 3 berkas), auditor 0 Critical, ketujuh gate exit 0 — diverifikasi ulang coordinator pada working tree final. Enam siklus sesudah siklus 1 seluruhnya mengejar satu kelas cacat: **guard yang cakupan atau sebabnya lebih sempit/berbeda daripada yang diklaim komentarnya** — kelas yang [ADR-0020](decisions.md#adr-0020) buktikan mahal. Yang ditutup: `dist/*.html` dipindai rekursif dengan daftar dokumen yang diharapkan plus asersi positif `<script type="module" src=`; batas bundle menjaga `src/output/**` **dan** `src/shared/**` atas sembilan ejaan (`.js`/`.mjs`/`.cjs`/`.jsx`/`.ts`/`.mts`/`.cts`/`.tsx`/`.vue`), statik, re-export, dan dinamis; himpunan ejaan itu kini sama persis di `eslint.config.js`, `tsconfig.json`, dan `vitest.config.ts`, **dan kesamaannya dikunci test** (`tests/unit/toolchain-file-coverage.test.js` — dijalankan terhadap HEAD ia gagal pada tiga hitungan); komposisi exit code guard diekstrak jadi fungsi murni beserta matriks `violations` × `unreadable` × `blank`; `.gitattributes` memperbaiki `npm run lint` yang **merah pada clean clone** (17 berkas ditolak `prettier --check`, dibuktikan lewat `git clone` HEAD ke luar repo). Dua batas dibiarkan terbuka secara sadar dan tercatat: [ADR-0021](decisions.md#adr-0021) keluasan pencocokan teks specifier, [ADR-0022](decisions.md#adr-0022) origin subresource belum diperiksa (**persyaratan yang dibawa** oleh item pertama yang menambah subresource ke `src/*.html`). **Terbuka, non-blocking:** `.cjs`/`.cts` tidak dijaga terhadap `require()`; root config di luar tiga yang dinamai tidak masuk `include` tsconfig; `.vue` di `COLLOCATED_TESTS` tapi tidak dikoleksi Vitest; `moduleResolution: "bundler"` vs Node ESM di `scripts/`; sinkronisasi `EXPECTED_DOCUMENTS` ↔ `vite.config.ts` masih konvensi; `src/shared/bindings/` diperiksa ulang di SETUP-05. **Satu-satunya dimensi yang tidak terkunci test: Prettier** — `.gitattributes` benar hari ini, tapi tidak ada yang memerah bila ia dihapus; gejalanya baru muncul di mesin orang berikutnya yang meng-clone. Riwayat lengkap tujuh siklus ada di tabel bawah. *(Riwayat siklus 1, dipertahankan apa adanya:)* Tester LULUS — ketujuh AC terbukti, nol test gagal; klaim [ADR-0020](decisions.md#adr-0020) diverifikasi ulang secara independen (bentuk lama `cargo fmt`/`cargo test` benar-benar exit 0 atas core yang rusak-format dan `#[test]` yang gagal). Auditor 0 Critical, **3 Warning masih terbuka** — ketiganya bertema sama, guard lebih sempit daripada yang diklaim komentarnya: (W1) `scripts/check-dist-html.js:28` `readdir` tanpa `recursive: true` dan tanpa daftar dokumen yang diharapkan, sehingga HTML bersarang tak terpindai dan entry yang hilang tetap hijau; (W2) `eslint.config.js:97` tidak melarang `src/shared/**` mengimpor `src/main/**`, jalur transitif ke bundle projector terbuka; (W3) `eslint.config.js:119` selector hanya menjaring `Literal`, sehingga ``import(`../main/views/${x}.vue`)`` lolos. **Siklus 2 berjalan (18:36):** pengguna memilih memperbaiki ketiganya, bukan menundanya lewat ADR. Sampai item ini selesai, sebagian perintah verifikasi belum tersedia. **Tiga persyaratan tambahan dari audit SETUP-02:** (a) assertion build-time atas `dist/*.html` — tolak `<style`, `style="`, `<script>` ber-isi; ini satu-satunya penjaga `style-src` ([ADR-0015](decisions.md#adr-0015)); (b) aturan ESLint `no-restricted-imports` melarang `src/output/**` mengimpor `src/main/**` — auditor menyatakan bila SETUP-03 selesai tanpa ini, temuannya naik jadi Warning; (c) wajibkan `npm ci`, bukan `npm install`; (d) pindahkan guard `custom-protocol` ke `#[cfg(all(not(debug_assertions), dev))]` + `compile_error!` dan tambahkan penanda silang di `[profile.release]` — [ADR-0018](decisions.md#adr-0018). |
| SETUP-04 | Skema SQLite + migrasi dari PRD Appendix A: WAL, `foreign_keys=ON`, `synchronous=NORMAL`, `cache_size=-8000`, FTS5 | M | `done` | 2026-08-12 | [ADR-0025](decisions.md#adr-0025) · [ADR-0026](decisions.md#adr-0026) · [ADR-0027](decisions.md#adr-0027) · [ADR-0028](decisions.md#adr-0028) · [ADR-0029](decisions.md#adr-0029) | **3 siklus.** Tester LULUS (19 test Rust dari **nol**, 166 test JS), auditor 0 Critical, ketiga Warning tuntas, verifikasi runtime oleh pengguna LULUS. Blocking untuk seluruh FR-2xx. **Item pertama yang membuat data jemaat benar-benar ada di disk** — SETUP-06 sengaja didahulukan untuk ini. Empat pragma Appendix A dipasang di satu-satunya konstruktor koneksi dan **dibaca balik**, karena SQLite menjawab pragma yang tak dapat dipenuhi dengan melaporkan nilai lama alih-alih error. Runner migrasi memeriksa urutan **saat kompilasi**, menulis `schema_migrations` + `user_version` dalam satu transaksi, dan menolak skema masa depan meniru FR-708. **Dibawa keluar:** [ADR-0027](decisions.md#adr-0027) → item logging/pelaporan galat (kegagalan `db::init` tak terlihat siapa pun); [ADR-0028](decisions.md#adr-0028) → item rilis pertama (guard checksum migrasi); [ADR-0029](decisions.md#adr-0029) tercatat (nol enkripsi at-rest, isolasi = akun OS); kolom path relatif tanpa `CHECK` → item FR-5xx yang mengisinya. **Tidak terkunci:** GATE-G10 (cakupan ≥80%) tidak diukur — nol tool coverage terpasang; `cargo audit` tidak terpasang, jadi advisory dependency baru diperiksa manual lewat pembacaan sumber, bukan basis data resmi. |
| SETUP-05 | Pembangkitan tipe Rust→TS (`ts-rs`/`specta`) + kerangka `tests/{unit,integration,perf}` | M | `todo` | — | [ADR-0019](decisions.md#adr-0019) · [ADR-0023](decisions.md#adr-0023) | **Persyaratan yang dibawa dari SETUP-06 — [ADR-0023](decisions.md#adr-0023):** item ini membuat direktori tempat `fixtures/` lahir, jadi guard-nya harus ada **sebelum** fixture pertama ditulis. Pengecualian `!**/fixtures/**/*.aero` mencabut satu-satunya sinyal yang menahan dokumen ibadah (keterabaian), dan justru fixture terpenting yang paling berisiko: korpus NFR-28 paling gampang dibuat dengan memutasi `.aero` sungguhan, dan FR-706 menyematkan teks lirik penuh. Butuh deteksi **positif** — fixture dibuktikan sintetis, bukan diasumsikan — dengan bentuk seperti `scripts/check-dist-html.js` (pemeriksa terikat lifecycle npm, bagian murni dapat diuji unit). Satu pemeriksa staged-content menutup tiga lubang sekaligus: fixture tercemar, `git add -f`, dan berkas yang terlanjur terlacak. **Sisanya:** NFR-33 — kontrak command/event tidak boleh ditulis ganda. **Batasan dari SETUP-02:** guard `custom-protocol` menolak seluruh profil rilis, jadi `cargo test --release` gagal. Harness `tests/perf/` (NFR-01…NFR-07, GATE-G1…G9) wajib memakai `cargo test --release -p aeroworship --features tauri/custom-protocol`; harness yang hanya menyentuh `aeroworship-core` tidak terpengaruh. Tetapkan perintahnya di satu tempat, jangan biarkan tiap orang menemukannya lewat pesan error yang menyesatkan. |
| SETUP-06 | Lengkapi `.gitignore`: `.env*`, `*.db` / `*-wal` / `*-shm` / `*.sqlite*`, `*.aero`, `*.aerotpl`, `*.log` | M | `done` | 2026-08-09 | [ADR-0023](decisions.md#adr-0023) · [ADR-0024](decisions.md#adr-0024) | **3 siklus.** Tester LULUS (162 test, 17 di antaranya milik item ini), auditor 0 Critical. Lima blok baru menutup secrets, artefak DB dev, log, dokumen pengguna, dan autosave; ditambah `/cache/` + `/media/` ber-anchor root untuk render visual khotbah, yang seringkali lebih terbaca daripada `.aero` yang hanya memuat UUID. Ketegangan pokok item ini — NFR-15 dan NFR-28 menuntut fixture `.aero` **ada di repo**, sementara judul item menuntut `*.aero` diabaikan — diselesaikan dengan penanda **nama direktori** (`!**/fixtures/**/*.aero`), sempit pada ekstensi dan luas pada lokasi, sehingga `.env`/`*.db`/`*.log` tetap terabaikan di dalam `fixtures/` dan fixture di `src-tauri/crates/*/tests/fixtures/` (konvensi Cargo) ikut terjangkau. Perilaku `.gitignore` dikunci test permanen yang menjalankan git sungguhan di repo throwaway. **Dibawa keluar:** [ADR-0023](decisions.md#adr-0023) → SETUP-05 (guard staged-content: isi fixture dibuktikan sintetis, bentuk salinan data root di lokasi mana pun, `git add -f`, berkas terlanjur terlacak); [ADR-0024](decisions.md#adr-0024) → item FR-410 (`*.aerotpl` sudah mengabaikan calon sumber template bawaan, dan suite justru menahan cacatnya); sibling atomic-write `session.autosave.json.tmp` → FR-707; penamaan rotasi log → item logging; `.env.example` senyap → item pertama yang memperkenalkan env. **Tidak terkunci test:** prosa komentar — nol test membacanya, jadi klaim yang salah hanya tertangkap manual, dan itu memang yang terjadi dua kali di item ini. *(Asal item:)* Temuan auditor S5 (siklus 1 SETUP-01, dinaikkan jadi item tersendiri). Berhenti jadi Suggestion dan mulai jadi risiko kebocoran data pengguna begitu SETUP-04 membuat database — database dev berisi lirik dan isi ibadah nyata. Jauh lebih murah dikerjakan **sebelum** berkas-berkas itu ada. Kerjakan sebelum SETUP-04. |
| SETUP-07 | **Verifikasi runtime pertama — dikerjakan pengguna, bukan agent** | M | `done` | 2026-08-09 | [ADR-0015](decisions.md#adr-0015) | Aplikasi belum pernah dijalankan sekali pun sejak commit pertama. Prasyaratnya kini lunas (`dist/` ada, installer terbangun). Langkah: pasang `src-tauri/target/release/bundle/nsis/AeroWorship_0.1.0_x64-setup.exe`, jalankan, konfirmasi window "AeroWorship" 1280×800 terbuka dengan teks "AeroWorship Control Panel" di atas latar `#16181d`, tanpa panic startup. **Harus build produksi, bukan `tauri dev`** — CSP tidak ditegakkan di mode dev ([ADR-0015](decisions.md#adr-0015)), jadi "console bersih" di sana tidak bermakna. Devtools juga tidak tersedia di rilis, sehingga yang bisa dikonfirmasi manual adalah "UI ter-render dengan gaya benar"; CSS yang gagal dimuat akan tampak sebagai halaman tanpa gaya. Blocking untuk [ADR-0015 S4](decisions.md#adr-0015) (`freezePrototype` menunggu baseline "aplikasi jalan"). |

---

## M1 — Foundation · FR-1xx Dual-Monitor System

*Exit criteria PRD §9: dua window di dua display; gate G1 mulai diukur dan dilacak sejak titik ini.*

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-101 | Enumerasi display saat startup via backend Rust (`list_monitors`) | M | `todo` | — | — | — |
| FR-102 | Auto-placement: Control Panel di display primer, Projector Output fullscreen borderless di display non-primer pertama | M | `todo` | — | — | — |
| FR-103 | Reassign display output manual, persisten per mesin | M | `todo` | — | — | — |
| FR-104 | Deteksi connect/disconnect runtime + event `monitor:changed`; state slide dipertahankan, output dipulihkan otomatis | M | `todo` | — | — | Terkait NFR-08; jalur ini rawan under-tested (PRD R4) |
| FR-105 | Window output tanpa chrome, title bar, scrollbar, context menu, seleksi teks, dan devtools di release build | M | `todo` | — | — | — |
| FR-106 | Fallback satu display: preview berjendela, tanpa fullscreen output | M | `todo` | — | — | — |
| FR-107 | State output `content` / `black` / `logo` / `clear`, masing-masing dapat dialamatkan independen | M | `todo` | — | — | — |
| FR-108 | Penanganan DPI campuran: output render pada resolusi piksel native display output | S | `todo` | — | — | — |
| FR-109 | Output always-on-top hanya di display-nya sendiri, tidak mencuri fokus keyboard | M | `todo` | — | — | — |

---

## M2 — Content core · FR-2xx Library + FR-4xx renderer inti

*Exit criteria PRD §9: gate G3 lulus; sebuah lagu tampil di projector melalui renderer sungguhan.*

### FR-2xx — Library Management

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-201 | Pencarian full-text inkremental via SQLite FTS5 (judul, penulis, isi lirik) | M | `todo` | — | — | Target NFR-03: p95 < 150 ms atas 10.000 lagu |
| FR-202 | CRUD lagu lengkap dengan soft-delete dan jendela pemulihan 30 hari | M | `todo` | — | — | — |
| FR-203 | Lagu disimpan sebagai kumpulan **section** berlabel, bukan blob teks tunggal | M | `todo` | — | — | Prasyarat FR-204 |
| FR-204 | **Arrangement** bernama: urutan referensi ke section; arrangement default dibuat otomatis | M | `todo` | — | — | — |
| FR-205 | Parser referensi kitab: nama penuh & singkatan ID/EN, pasal:ayat, rentang, rentang lintas pasal | M | `todo` | — | — | Fungsi murni di Rust, wajib unit-test (PRD §6.1) |
| FR-206 | Gagal parse referensi → jatuh diam-diam ke full-text search atas teks ayat | M | `todo` | — | — | Jalur gagal ini wajib punya test tersendiri |
| FR-207 | Dukungan banyak versi Alkitab; ganti versi tanpa mengubah referensi | S | `todo` | — | — | — |
| FR-208 | Impor versi Alkitab dari format interchange; kirimkan minimal satu versi public-domain | M | `todo` | — | [ADR-0005](decisions.md#adr-0005) | 31.102 ayat < 60 detik + cek integritas jumlah ayat |
| FR-209 | Tag dan filter lintas tipe konten | C | `todo` | — | — | — |
| FR-210 | Registrasi aset media di library dengan dimensi, ukuran, dan content hash | M | `todo` | — | — | BLAKE3 |
| FR-211 | Penanda tipe konten pada hasil pencarian, tidak bergantung warna | S | `todo` | — | — | Aksesibilitas, terkait NFR-35 |

### FR-4xx — Template inti (sisanya di M5)

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-401 | Template sebagai JSON deklaratif berversi; tanpa kode yang dapat dieksekusi | M | `todo` | — | — | Input tidak tepercaya (NFR-28); node script ditolak saat load |
| FR-402 | Layer background: warna solid, gradasi linear, atau gambar (cover/contain/stretch/tile) | M | `todo` | — | — | — |
| FR-403 | Layer shape: rect, rounded rect, path SVG arbitrer — fill, opacity, stroke, blur | M | `todo` | — | — | Path SVG divalidasi terhadap allowlist grammar (PRD §6.7) |
| FR-404 | Slot teks terikat role `primary`/`secondary`/`reference`/`attribution` + properti tipografi | M | `todo` | — | — | — |
| FR-405 | Live preview < 200 ms, memakai renderer yang sama persis dengan output | M | `todo` | — | — | Klaim pixel-identity hanya benar bila renderer tunggal (PRD §6.7) |
| FR-406 | Seluruh geometri dalam satuan ternormalisasi 0–1 relatif kanvas | M | `todo` | — | — | Dasar independensi resolusi |
| FR-410 | Sertakan minimal tiga template bawaan yang tidak dapat dihapus | M | `todo` | — | — | — |

---

## M3 — Workflow · FR-3xx Session & Live Presentation

*Exit criteria PRD §9: gate G2 lulus; satu ibadah penuh dapat dijalankan ujung ke ujung.*

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-301 | Aplikasi terbuka dengan Session panel kosong + hint inline; tidak ada auto-load | M | `todo` | — | — | — |
| FR-302 | Daftar "Recent sessions" sebagai aksi opt-in eksplisit | S | `todo` | — | — | — |
| FR-303 | Drag-and-drop Library → Session dengan indikator posisi sisip | M | `todo` | — | — | Drop-to-render ≤ 50 ms; duplikat diizinkan |
| FR-304 | Reorder via drag; hapus via `Delete` dengan undo 8 detik | M | `todo` | — | — | — |
| FR-305 | Override template per item, fallback ke default tipe lalu default global | S | `todo` | — | — | — |
| FR-306 | Navigasi slide: next, previous, first, last, jump-to-index — seluruhnya keyboard | M | `todo` | — | — | Tunduk pada NFR-02 |
| FR-307 | Panic control: blank (`B`), logo (`L`), clear (`C`) + indikator status persisten | M | `todo` | — | — | ≤ 100 ms; indikator harus terbaca sekilas di ruang gelap |
| FR-308 | Pemisahan Preparation Mode / Live Mode; Live Mode tanpa afordans penyuntingan sama sekali | M | `todo` | — | — | Diferensiator D3; tidak boleh dilonggarkan |
| FR-309 | Beralih Live → Preparation saat konten live memerlukan konfirmasi eksplisit | M | `todo` | — | — | — |
| FR-310 | Pemecahan slide otomatis pada batas baris, deterministik dan identik di preview dan output | M | `todo` | — | — | Fungsi murni Rust, wajib unit-test |
| FR-311 | Control Panel menampilkan preview slide aktif dan slide berikutnya bersamaan | S | `todo` | — | — | Harus muat di 1366×768 tanpa scroll (NFR-19) |
| FR-312 | Item session dapat berupa lagu, ayat, deck impor, gambar, atau penanda kosong | M | `todo` | — | — | — |
| FR-313 | Navigasi live melintasi batas item tanpa dead-end | M | `todo` | — | — | — |

---

## M4 — Portability · FR-7xx `.aero` Session Files

*Exit criteria PRD §9: session yang dibangun di mesin A tampil di mesin B.*

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-701 | Ekspor session ke `.aero`: JSON UTF-8 sesuai Appendix C, dengan `schema_version` | M | `todo` | — | — | Berkas ibadah tipikal < 100 KB |
| FR-702 | Referensi entitas via UUID + field fallback human-readable terdenormalisasi | M | `todo` | — | — | — |
| FR-703 | Impor `.aero`; item yang tak terselesaikan **tidak** menggagalkan pemuatan | M | `todo` | — | — | Jalur gagal, wajib punya test |
| FR-704 | Aksi resolusi eksplisit untuk item tak terselesaikan: cari, buat dari fallback, hapus | M | `todo` | — | — | — |
| FR-705 | Path media relatif + fallback absolut + hash BLAKE3; relink-by-hash saat gagal | M | `todo` | — | — | — |
| FR-706 | Portable export opsional: sematkan teks lirik untuk mesin dengan library kosong | S | `todo` | — | — | 6 lagu tetap < 200 KB |
| FR-707 | Autosave tiap 30 detik dan tiap mutasi; tawarkan pemulihan setelah terminasi abnormal | S | `todo` | — | — | NFR-09 |
| FR-708 | Tolak `schema_version` masa depan dengan pesan spesifik; migrasi versi lama ke depan | M | `todo` | — | — | Tidak boleh muncul sebagai parse error |
| FR-709 | Registrasi asosiasi berkas `.aero` saat instalasi | C | `todo` | — | — | — |

---

## M5 — Import & polish · FR-5xx, FR-4xx sisa, FR-6xx

*Exit criteria PRD §9: seluruh FR prioritas Must selesai.*

### FR-5xx — Presentation Import

Komponen berisiko tertinggi di MVP (PRD R1). Jangan mulai sebelum [ADR-0004](decisions.md#adr-0004) diputuskan.

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-501 | Impor `.ppt` / `.pptx` via file picker atau drag-and-drop ke Library | M | `todo` | — | [ADR-0004](decisions.md#adr-0004) | — |
| FR-502 | Konversi berjalan penuh di worker thread Rust, tidak memblokir UI | M | `todo` | — | — | Frame timing Control Panel < 16,7 ms p95 selama konversi |
| FR-503 | Rasterisasi tiap slide ke WebP q90 pada resolusi output, fallback PNG | M | `todo` | — | — | — |
| FR-504 | Event `import:progress` + progress bar determinate + pembatalan | M | `todo` | — | — | Pembatalan tidak boleh meninggalkan baris parsial atau berkas yatim |
| FR-505 | Thumbnail 320 px per slide untuk session grid | M | `todo` | — | — | — |
| FR-506 | Berkas sumber tidak diperlukan setelah impor; catat path, hash, tanggal | M | `todo` | — | — | Properti yang membuat alur flash-drive aman |
| FR-507 | Deteksi deck usang (hash sumber berubah) dan tawarkan impor ulang | C | `todo` | — | — | — |
| FR-508 | Toolchain konversi tidak tersedia → pesan spesifik menyebut dependensi dan solusinya | M | `todo` | — | [ADR-0004](decisions.md#adr-0004) | Tidak boleh berupa kegagalan generik |
| FR-509 | Impor PDF melalui jalur rasterisasi yang sama | S | `todo` | — | [ADR-0004](decisions.md#adr-0004) | Jalur tanpa dependensi eksternal sama sekali |

### FR-4xx — Template Builder (sisa)

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-407 | Safe-area guide + indikator overflow saat konten tidak muat pada ukuran font minimum | S | `todo` | — | — | — |
| FR-408 | Resolusi template global → tipe konten → item; read-only di Live Mode | M | `todo` | — | — | — |
| FR-409 | Duplicate, rename, delete, export, import template `.aerotpl` | S | `todo` | — | — | Gambar hilang memicu alur relink |

### FR-6xx — Online Lyric Search

Seluruh blok ini bergantung pada [ADR-0006](decisions.md#adr-0006). Bila provider tidak lolos tinjauan ToS, blok ini ditunda pasca-MVP — kecuali FR-606, yang tetap wajib.

| ID | Judul | Pri | Status | Diperbarui | Keputusan | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| FR-601 | Pencarian online sebagai tab sekunder terpisah; hasil lokal dan online tidak pernah dicampur | S | `todo` | — | [ADR-0006](decisions.md#adr-0006) | — |
| FR-602 | Deteksi konektivitas via request nyata, lazy, tidak pernah memblokir startup atau pencarian lokal | S | `todo` | — | [ADR-0006](decisions.md#adr-0006) | — |
| FR-603 | Provider di balik trait Rust; timeout 5 detik dan dapat dibatalkan | S | `todo` | — | [ADR-0006](decisions.md#adr-0006) | Provider macet tidak boleh menggantung UI |
| FR-604 | Pratinjau sebelum impor; konfirmasi eksplisit; label section dapat disunting | S | `todo` | — | [ADR-0006](decisions.md#adr-0006) | — |
| FR-605 | Persistensi provenance: source URL, nama provider, timestamp; field CCLI tersedia | S | `todo` | — | [ADR-0006](decisions.md#adr-0006) | Terkait risiko hak cipta PRD R3 |
| FR-606 | Akses jaringan opt-in per query; nol request keluar tanpa inisiasi pengguna | M | `todo` | — | — | **Tetap wajib meski FR-601…605 ditunda.** Diverifikasi dengan packet capture |

---

## M6 — RC & Validation · Release Gates

Dari PRD §7.1. Gate berlabel *blocker* menahan rilis MVP.

| ID | Gate | Target | Blocker | Status | Diperbarui | Catatan |
| --- | --- | --- | --- | --- | --- | --- |
| GATE-G1 | RAM standby seluruh proses | ≤ 100 MB | Ya | `todo` | — | Diukur dan dilacak sejak M1 selesai |
| GATE-G2 | Latensi transisi slide p95 | < 100 ms | Ya | `todo` | — | Divalidasi silang sekali dengan kamera 240 fps |
| GATE-G3 | Latensi pencarian p95, 10k lagu | < 150 ms | Ya | `todo` | — | — |
| GATE-G4 | Cold start (HDD) | < 3 s | Ya | `todo` | — | — |
| GATE-G5 | Ukuran installer | < 15 MB | Ya | `todo` | 2026-08-08 | **Angka pertama yang sah: installer NSIS ~1,39 MB — 9,3% dari anggaran 15 MB.** Terukur tiga kali: 1.393.858 / 1.394.695 / 1.394.292 byte; selisih < 900 byte = nondeterminisme NSIS, jadi pakai sebagai kisaran, bukan nilai tunggal. `aeroworship.exe` 5.132.800 byte, cocok byte-for-byte di dua pengukuran independen. Hanya sah bila diukur dari `npm run tauri build` — lihat [ADR-0016](decisions.md#adr-0016)/[0017](decisions.md#adr-0017); angka 5.078.016 di [ADR-0012](decisions.md#adr-0012) adalah biner ber-flavour dev dan **bukan** pembanding. Belum memuat bootstrapper WebView2 ([ADR-0011](decisions.md#adr-0011)) dan belum memuat dependency M2–M5. Tegangan dengan [ADR-0004](decisions.md#adr-0004). |
| GATE-G6 | Request keluar selama ibadah offline | 0 | Ya | `todo` | — | Packet capture |
| GATE-G7 | Fault window output tidak memengaruhi Control Panel | Lulus | Ya | `todo` | — | Fault injection |
| GATE-G8 | Soak 2 jam, pertumbuhan RSS | < 5% | Ya | `todo` | — | — |
| GATE-G9 | CPU idle | < 1% | Tidak | `todo` | — | Menuntut nol animation loop dan nol polling timer saat idle |
| GATE-G10 | Cakupan unit-test core Rust | ≥ 80% | Tidak | `todo` | — | — |

Validasi usabilitas (NFR-22 SUS > 75, NFR-23 UEQ) memerlukan studi dengan n ≥ 12 operator dan tidak dapat diotomatisasi. Lihat PRD §7.3.

---

## Riwayat perubahan status

`project-lead` menambahkan satu baris di sini setiap kali sebuah item berpindah status. Append-only.

| Tanggal | Item | Dari | Ke | Catatan |
| --- | --- | --- | --- | --- |
| 2026-08-07 | — | — | — | PROGRESS.md dibuat dari docs/PRD.md v1.0. Seluruh item `todo`. |
| 2026-08-07 | SETUP-01 | `todo` | `in-progress` | Prasyarat toolchain diverifikasi. Didelegasikan ke implementer. |
| 2026-08-07 | SETUP-01 | `in-progress` | `in-progress` | Siklus 1: implementer → tester (LULUS) → auditor (0 Critical). Tetap `in-progress`, bukan kegagalan. Biaya siklus edit-test terukur: cold 37m12s, warm 13s, inkremental 1m22s pada crate 26 baris. |
| 2026-08-08 | SETUP-01 | `in-progress` | `in-progress` | Siklus 2: workspace 2 crate ([ADR-0008](decisions.md#adr-0008)), `panic="unwind"` ([ADR-0009](decisions.md#adr-0009)), `core:window:default` dihapus. Tester LULUS — isolasi crate core terbukti (target dir kosong → 4,78 s, nol artefak Tauri), `panic="unwind"` terbukti empiris via kontrol A/B, biner release 5,42 MB. Auditor **mencabut W4** (premis keliru, `reqwest` target-gated Android/iOS) dan menemukan Warning baru: feature `dynamic-acl` aktif → [ADR-0013](decisions.md#adr-0013). Angka ADR-0008 dikoreksi di [ADR-0012](decisions.md#adr-0012). |
| 2026-08-08 | SETUP-01 | `in-progress` | **`done`** | Siklus 3: `dynamic-acl` dimatikan ([ADR-0013](decisions.md#adr-0013)). Tester LULUS — daftar feature = default minus tepat satu, `dynamic-acl` terbukti configured out lewat diagnostik rustc, biner turun 603.648 byte (A/B byte-per-byte). Auditor 0 Critical; Warning baru [ADR-0014](decisions.md#adr-0014) (sisa `__allow_command`/`runtime_authority_mut`, tidak dapat ditutup lewat feature flag) ditunda sadar. Syarat penutupan auditor dipenuhi: ADR-0014 ditulis, SETUP-06 ditambahkan. |
| 2026-08-08 | SETUP-02 | `todo` | `in-progress` | Didelegasikan ke implementer. Membawa utang verifikasi runtime SETUP-01. |
| 2026-08-08 | SETUP-02 | `in-progress` | **`done`** | 2 siklus. Siklus 1: Vite dua entry. Siklus 2: guard `custom-protocol` ([ADR-0016](decisions.md#adr-0016)/[0017](decisions.md#adr-0017)), `style-src 'self'` ([ADR-0015](decisions.md#adr-0015)), `server.fs.allow`, hapus `@tauri-apps/api`. Tester LULUS 41 pemeriksaan; auditor 0 Critical / 0 Warning, W1 tertutup. Empat koreksi tercatat: resep ADR-0016 keliru → [ADR-0017](decisions.md#adr-0017); penilaian biaya ADR-0017 keliru → [ADR-0018](decisions.md#adr-0018); `withGlobalTauri: false` bukan batas IPC → [ADR-0018](decisions.md#adr-0018); guard menolak `cargo test --release` → [ADR-0019](decisions.md#adr-0019). |
| 2026-08-08 | SETUP-03 | `todo` | `in-progress` | Didelegasikan ke implementer. **Dipilih mendahului SETUP-06** meski SETUP-06 lebih murah: risiko kebocoran yang dijaga SETUP-06 belum ada wujudnya (nol berkas `.db`/`.aero`/`.env` di repo, dan tidak ada kode yang membuatnya sampai SETUP-04), sedangkan ADR-0015 hari ini **tidak punya penjaga sama sekali** — assertion `dist/*.html` adalah satu-satunya, dan ia belum ada. SETUP-06 tetap dikerjakan tepat setelah ini, sebelum SETUP-04. |
| 2026-08-08 16:43 | SETUP-03 | `in-progress` | `in-progress` | Siklus 1 diverifikasi. Tester LULUS ketujuh AC — AC-6 dibuktikan dua arah dengan build rilis penuh (tanpa feature: exit 101 dalam 4,5 s; dengan `--features tauri/custom-protocol`: exit 0 dalam 2m07s). Auditor 0 Critical, 3 Warning terbuka (W1 `check-dist-html.js` tidak rekursif; W2 `src/shared/**` tak terjaga; W3 selector dynamic import hanya `Literal`) + 8 Suggestion. Coordinator menutup S6 secara langsung: `npm audit` 0 vulnerability dan `obug@2.1.4` terbukti fork sah `debug` oleh `sxzz`, bukan typosquat; residu uji coba `is-odd` dihapus dari `devDependencies`. **Belum `done` — H2 menuntut Warning diperbaiki atau ditunda lewat entri `decisions.md`.** Tiga temuan tester yang tidak memblokir dicatat untuk item berikutnya: `tests/**` tidak terjaring `vitest.config.ts` (kelas kegagalan sama dengan ADR-0020, menjadi nyata di SETUP-05), `eslint.config.js` satu-satunya berkas tooling di luar `include` `tsconfig.json`, dan `import('../main')` tanpa slash penutup belum terjaring. |
| 2026-08-08 17:15 | SETUP-03 | `in-progress` | `in-progress` | **Dijeda atas permintaan pengguna — bukan kegagalan, bukan blocked.** Pekerjaan siklus 1 di-commit sebagai checkpoint. **Keputusan yang menunggu jawaban pengguna:** tiga Warning auditor (W1/W2/W3) diperbaiki di siklus 2, atau ditunda sadar lewat entri `decisions.md` sesuai H2. Rekomendasi coordinator: siklus 2 — ketiganya murah, dan ketiganya bertema sama yaitu komentar mengklaim cakupan lebih luas daripada kodenya, kelas kegagalan yang baru saja terbukti mahal di ADR-0020. Di luar itu, `.claude/agents/{project-lead,implementer,tester}.md` diperbaiki ke bentuk `--all`/`--workspace` (temuan tester no. 4; ADR-0020 melarang bentuk lama dikutip di definisi agent), dan `project-lead.md` mendapat aturan keras baru **H6** atas permintaan pengguna: tulis ke `PROGRESS.md` sebelum mendelegasikan, bukan sesudahnya. |
| 2026-08-08 | SETUP-07 | — | `todo` | Item baru. Utang verifikasi runtime dipisahkan dari SETUP-02 agar punya pemilik (pengguna) dan status sendiri, bukan terus dititipkan ke item berikutnya. |
| 2026-08-08 18:36 | SETUP-03 | `in-progress` | `in-progress` | **Siklus 2 dimulai.** Pengguna menjawab jeda 17:15 dengan "lanjutkan" — artinya opsi yang direkomendasikan: perbaiki ketiga Warning, bukan menundanya lewat ADR. Didelegasikan ke implementer. Cakupan: W1 (`check-dist-html.js` rekursif + daftar dokumen yang diharapkan), W2 (`src/shared/**` dilarang mengimpor `src/main/**`), W3 (selector dynamic import menjaring `TemplateLiteral` dan bentuk tanpa slash penutup), plus dua celah tooling sekelas ADR-0020 yang ditemukan tester: `tests/**` tidak terjaring `vitest.config.ts` dan `eslint.config.js` di luar `include` `tsconfig.json`. Kelima-limanya satu tema — guard yang diam-diam lebih sempit daripada klaimnya — dan itu alasan dikerjakan bersama, bukan dipecah. Setelah implementer: tester + security-auditor paralel, seperti siklus 1. |
| 2026-08-08 20:55 | SETUP-03 | `in-progress` | `in-progress` | **Implementer siklus 2 selesai; verifikasi diulang setelah session limit.** Implementer menutup kelimanya di empat berkas (`check-dist-html.js`, `eslint.config.js`, `vitest.config.ts`, `tsconfig.json`), tanpa dependency baru, tanpa menyentuh `src-tauri/` atau `src/`; delapan probe positif menyala satu per satu dan kontrol negatif (`domain/`, `main-thread`, `maintenance`) nol error. Seluruh gate hijau. **Satu koreksi atas brief coordinator:** klaim bahwa backslash dari `readdir(recursive)` merusak `new URL()` **tidak benar** — diukur di Node 22.16, WHATWG memperlakukan `\` sebagai `/` untuk special scheme dan `file:` termasuk. Normalisasi dipertahankan dengan alasan lain yang benar-benar berlaku. Tester dan auditor dijalankan paralel pukul ~19:10 tetapi **keduanya mati oleh session limit sebelum menyentuh apa pun** — working tree diverifikasi utuh (lima berkas, nol residu probe), jadi ini bukan kegagalan siklus dan tidak dihitung terhadap H3. Keduanya didelegasikan ulang dengan brief yang sama. |
| 2026-08-08 21:20 | SETUP-03 | `in-progress` | `in-progress` | **Siklus 2 diverifikasi: tester GAGAL 1 criterion, auditor 0 Critical. Ini kegagalan ke-1 pada item ini (H3); satu lagi → `blocked`.** Ketiga Warning siklus 1 dinyatakan **TERTUTUP** auditor dengan bukti per baris, dan tester mengunci W2/W3 secara permanen lewat 39 test baru di `tests/unit/eslint-bundle-boundary.test.js` — dijalankan terhadap `eslint.config.js` versi HEAD, 16 di antaranya gagal, jadi test-nya load-bearing. Kedua klaim implementer dikonfirmasi independen (separator Windows; T2 tanpa escape hatch). **Yang gagal:** kontrol negatif — `./main`, `./widgets/main/x`, `somelib/main` terjaring padahal tak menyentuh `src/main/`, sementara JSDoc `eslint.config.js:37` mengklaim "resolve into `src/main/`". Dampak nyata nol; yang salah klaimnya, bukan perilakunya → diselesaikan lewat [ADR-0021](decisions.md#adr-0021): keluasan diterima sadar, komentar dan pesan dikoreksi. **Tiga Warning baru auditor:** N1 `check-dist-html.js:33,74-87` guard `EXPECTED_DOCUMENTS` tak punya test dan tak bisa diberi test (`main()` tak diekspor) — tester tiba di kesimpulan sama secara independen; N2 `tests/**` masuk Vitest tapi tak masuk `include` `tsconfig.json`; N3 kedua blok batas bundle dan `include` `src/` sama-sama berhenti di `{ts,vue}`, sehingga satu berkas `src/shared/*.js` melewati ESLint **dan** `vue-tsc` lalu tetap di-bundle Vite. Siklus 3 didelegasikan untuk kelimanya. |
| 2026-08-08 21:52 | SETUP-03 | `in-progress` | `in-progress` | **Implementer siklus 3 selesai; tester + auditor didelegasikan.** Lima berkas tooling diubah, nol dependency baru, `src-tauri/` dan `src/` tidak disentuh. N1: `selectDocuments(entries, expected)` diekstrak ke `dist-html-guard.js` sebagai fungsi murni — implementer menyatakan sendiri bahwa **N1 belum tertutup sampai tester menulis kasusnya**, karena hari ini menghapus perhitungan `missing` masih meninggalkan keenam gate hijau. N3 dibuktikan empiris dua arah: probe `src/shared/probe-boundary.js` kini menggagalkan **ESLint dan `vue-tsc`**, sebelumnya lolos keduanya. N2: `tests/**` masuk `include` tsconfig dan blok `globals.node`; berkas test milik tester lolos typecheck tanpa disentuh. D: JSDoc dan kedua pesan dikoreksi sesuai [ADR-0021](decisions.md#adr-0021). S4 ditutup lewat `try`/`catch` alih-alih penyaringan `Dirent`, agar tanda tangan `selectDocuments` tetap murni — konsekuensinya (direktori ber-nama `.html` ikut terpilih, pemanggil yang menerjemahkan kegagalan baca) dinyatakan eksplisit di JSDoc, dan terbukti: direktori `dist/probe.html/` menghasilkan diagnostik bernama, bukan stack trace. `npm test` tetap 58. **Catatan implementer untuk auditor berikutnya:** `.jsx`/`.tsx` masih lolos kedua guard — lubang sekelas N3 untuk ekstensi lain, sengaja tidak ditambal diam-diam karena di luar teks brief dan JSX belum dikonfigurasi di Vite. |
| 2026-08-09 04:08 | SETUP-03 | `in-progress` | `in-progress` | **Auditor siklus 3: 0 Critical, 2 Warning, 7 Suggestion. Tester siklus 3 mati oleh session limit sebelum menulis satu test pun — working tree diverifikasi bersih, tidak dihitung sebagai kegagalan H3 (posisi tetap 1 kegagalan).** N2 tertutup penuh; N1 tertutup **hanya secara struktural** (`selectDocuments` terbukti murni: nol `fs`, nol state, tidak memutasi argumen — tetapi nol test menyentuhnya); N3 tertutup sebagian. Matriks exit code `failures` × `unreadable` ditelusuri keempatnya: bersih, `failures=0 ∧ unreadable>0` tetap exit 1 dan pesan "clean" tidak pernah tercetak. **W1:** auditor menolak penilaian implementer atas `.jsx`/`.tsx` — yang membuat berkas lolos adalah **ekstensinya, bukan sintaksnya**; ditambah `.mts`/`.cts` yang belum disebut siapa pun (glob `src/**/*.ts` tidak cocok dengan `x.mts`, sementara `resolve.extensions` Vite dan filter esbuild `\.(m?ts\|[jt]sx)$` memuatnya). JSDoc `BUNDLED_SOURCES` mengklaim "Everything Vite will pull into a bundle" di atas himpunan yang tidak total. **W2:** `vitest.config.ts` mengoleksi `scripts/**/*.test.ts` sementara `include` tsconfig berhenti di `scripts/**/*.js`; ejaan `src/**` sudah diseragamkan siklus 3, `scripts/**` tertinggal. Auditor menurunkan S1 siklus 2 ke "tercatat" karena test tester melinting lewat instance ESLint atas config asli. **Siklus 4 didelegasikan ke implementer sebelum tester dijalankan ulang**, agar tester tidak mensertifikasi config yang sudah pasti berubah: W1, W2, plus S1/S3/S4/S5/S6/S7 siklus 3. |
| 2026-08-09 05:00 | SETUP-03 | `in-progress` | `in-progress` | **Implementer siklus 4 selesai — kedelapan butir ditutup, tetapi dua gate merah ditemukan dan dikonfirmasi ulang oleh coordinator.** W1 dibuktikan pada lima ejaan: sebelum perubahan hanya `.cjs` terjaring, sesudahnya `.mts`/`.cts`/`.jsx`/`.tsx`/`.cjs` semuanya menggagalkan ESLint **dan** `vue-tsc`; `vite build` dikonfirmasi memancarkan isi kelimanya, jadi tidak ada ejaan yang layak dilarang alih-alih dijaga. S4 ditutup lewat `summariseDocuments` murni (bukan ekspor `main`, karena `main` tetap menuntut `dist/` sungguhan). S5 memakai asersi positif `<script type="module" src=` yang bentuknya diambil dari `dist/*.html` hasil build nyata — `crossorigin` menyisip di antara `type` dan `src`, jadi pemindainya urutan-agnostik. S6 dibuktikan: `new URL('a%41.html', …)` me-resolve ke `dist\aA.html`, `join()` tidak. **Dua gate merah:** (1) `npm test` **57/58** — satu test milik tester timeout pada 5000 ms; **pra-ada, bukan regresi** (baseline HEAD 11660 ms vs versi siklus 4 9054 ms — config baru justru lebih murah), dikonfirmasi ulang coordinator; (2) **`npm run lint` merah pada clean clone** — `core.autocrlf=true`, `.gitattributes` tidak ada, `.prettierrc.json` tidak menyetel `endOfLine` sehingga default `lf`, jadi berkas hasil checkout keluar CRLF dan `prettier --check` menolaknya; dikonfirmasi ulang coordinator. Keduanya cacat SETUP-03 itu sendiri, bukan item lain: toolchain kualitas yang merah di tangan orang yang baru meng-clone. Siklus 5: `.gitattributes` ke implementer, timeout ke tester (berkasnya sendiri). **Dicatat implementer, tidak ditambal:** `.cjs`/`.cts` dijaga terhadap `import`, bukan `require` — di kedua ejaan itu sintaks ESM adalah parse error, jadi bentuk yang benar-benar terpakai justru yang tak terjaring. |
| 2026-08-09 05:35 | SETUP-03 | `in-progress` | `in-progress` | **`.gitattributes` dibuat; klaim "merah pada clean clone" terbukti langsung, bukan tidak langsung.** Implementer memakai `git checkout-index -a --prefix=<scratchpad>/` — membaca dari index dan menulis ke luar repo dengan smudge filter tetap berlaku, jadi kondisinya persis clone baru tanpa menyentuh working tree yang memuat empat siklus pekerjaan belum ter-commit. Hasil: sebelum `.gitattributes` `prettier --check` **gagal atas 17 berkas** (yaitu seluruh berkas yang boleh dilihat Prettier), sesudahnya exit 0. Aturan `* text=auto eol=lf` — `text=auto` saja tidak cukup karena ia hanya memaku index dan menyerahkan checkout ke `core.autocrlf`. Pengecualian biner **diverifikasi, bukan diasumsikan**: `git check-attr` menunjukkan PNG/ICO tetap mewarisi `eol: lf` dari aturan `*` dan yang menyelamatkan adalah `text: unset` dari makro `binary`, lalu hash blob tersimpan vs hasil checkout dibandingkan untuk tiga berkas ikon — identik. Nol berkas wajib-CRLF di repo (template NSIS di-generate ke `gen/`/`target/` yang gitignored). **Implementer menolak menjalankan `git add --renormalize .`** dan saya setuju: disimulasikan dulu terhadap salinan index (`GIT_INDEX_FILE`), hasilnya nol perubahan akhir baris karena index sudah `i/lf` untuk setiap berkas teks — satu-satunya efek nyatanya men-stage tujuh berkas milik siklus 2–4 ke index. Manfaat nol, risiko kebingungan nyata. Peringatan `"LF will be replaced by CRLF"` kini nol. **Sisa yang dicatat, tidak ditambal:** `decisions.md` (`w/mixed`) dan `src-tauri/crates/core/src/lib.rs` (`w/crlf`) belum LF di working tree; keduanya tidak memerahkan gate mana pun (`.prettierignore` mencakup `*.md` dan `src-tauri/`; `rustfmt` default `newline_style = Auto`) dan akan menjadi LF sendiri pada checkout berikutnya. `npm test` masih 57/58 — didelegasikan ke tester. |
| 2026-08-09 10:36 | SETUP-03 | `in-progress` | `in-progress` | **Auditor siklus 4–5: 0 Critical, 2 Warning, 5 Suggestion, dan penilaian eksplisit "layak ditutup setelah W1 diperbaiki, tidak perlu siklus keenam".** W1 dan W2 siklus 3 dinyatakan TERTUTUP, diperiksa entry-per-entry (sembilan ejaan ESLint = sembilan entry tsconfig untuk `src/**`, delapan untuk `scripts/**`); kedisjoinan globals eksak; `.gitattributes` dinilai benar termasuk mekanisme `text: unset` dari makro `binary`, dan tidak ada biner terlacak lain di repo. **Dua Warning baru:** (W1) `eslint.config.js:83-94` — perbaikan S3 siklus 4 menukar satu ketidakakuratan dengan yang lain: pemetaan "first"/"second" terbalik dari urutan paragrafnya sendiri, sehingga orang yang kelak ingin menutup `import('../main/' + name)` akan menyimpulkan mengganti `>` dengan descendant tidak berguna, padahal justru itu yang menutupnya; (W2) `dist-html-guard.js:281` — `hasModuleScript` menerima `src` **remote**, dan `findCspViolations` tidak memeriksa `<link rel="stylesheet" href="https://…">`; menyentuh NFR-13, ditunda sadar lewat [ADR-0022](decisions.md#adr-0022). **Tester mati oleh session limit untuk ketiga kalinya, tetapi kali ini setelah menyelesaikan P0 dan P1** — prioritas P0–P4 di brief terbukti berguna. Diverifikasi coordinator secara langsung: `npm test` **139 lulus** (naik dari 58), kegagalan timeout hilang; tester memperbaikinya lewat `beforeAll` berbudget 120 s dengan catatan terukur (panggilan pertama mahal, sesudahnya 33–200 ms) dan menolak menaikkan `testTimeout` global. 22 blok test baru untuk `selectDocuments` / `hasModuleScript` / `summariseDocuments`, termasuk yang tidak diminta eksplisit (argumen tidak dimutasi; direktori ber-nama `.html` ikut terpilih). `npm run lint`, `typecheck`, `build` + `postbuild` semuanya hijau. **Belum ada laporan tester, jadi H2 belum terpenuhi.** Siklus 6: implementer memperbaiki W1 (komentar), lalu tester dijalankan ulang dengan brief menyempit ke P2 sisa, P3, P4. |
| 2026-08-09 10:52 | SETUP-03 | `in-progress` | `in-progress` | **W1 diperbaiki dengan pengukuran, bukan penalaran — dan pengukurannya menemukan lebih dari yang diminta.** Implementer menjalankan sembilan bentuk lewat Linter API dengan dua selector berdampingan (`ImportExpression > Literal` vs descendant). Hasilnya menegaskan dugaan auditor: melonggarkan `>` **memang** menjangkau golongan B (`import('../main/' + name)` 0→1, ternary 0→2, `as string` 0→1) dan **tidak pernah** menjangkau golongan A. Sekaligus menemukan alasan sesungguhnya `>` layak dipertahankan, yang selama ini tidak tertulis di mana pun: descendant menjaring **setiap** `Literal` di bawah `ImportExpression`, bukan hanya teks specifier — terbukti empat false positive, di antaranya `import(name === 'main' ? './a.vue' : './b.vue')` dan **argumen kedua** `import('./x.json', { with: { type: 'main' } })`. Dua ditulis di komentar demi keringkasan. Nol perubahan perilaku; selector, `MAIN_IMPORT_GROUPS`, dan seluruh pesan tidak disentuh. `npm test` tetap 139, lint dan typecheck hijau. Tersisa satu syarat H2: **laporan tester**. |
| 2026-08-09 11:30 | SETUP-03 | `in-progress` | `in-progress` | **Tester LULUS — 145 test, nol gagal, ketujuh gate exit 0.** Sisa P2 terbukti: sepuluh probe (5 ejaan × 2 direktori, basename berbeda) semuanya dinamai `vue-tsc`, nol `TS2307`. Jebakan basename dikonfirmasi nyata alih-alih diterima: `twin.cts`+`twin.cjs` bercacat tipe → hanya `.cts` dilaporkan, yang JS dikeluarkan dari program tanpa suara. **P3 terjawab: auditor benar, `@/main` dan `@/main/**` redundan tanpa syarat** — diukur lewat Linter API dengan daftar pola identik sehingga `eslint.config.js` tidak perlu disentuh; `**/main` yang tidak ter-anchor sudah mencocokkan ketiga bentuk alias, dan karena rule ini mencocokkan teks specifier dan tidak pernah meresolusi (ADR-0021), memperkenalkan alias `@` di masa depan tidak mengubah apa pun. Tabel selector siklus 6 diukur ulang penuh (bukan dua baris yang diminta): kedelapan angka cocok. S6 terbukti dengan berkas nyata: `new URL` membaca `aA.html`, `join` membaca `a%41.html`. **`.gitattributes` diverifikasi dua arah dengan metode yang lebih baik dari yang saya sarankan** — `git checkout-index` ternyata tidak bisa mereproduksi kondisi "sebelum" karena git membaca `.gitattributes` dari working tree meski untracked; tester memakai `git clone` HEAD ke scratchpad: tanpa `.gitattributes` `prettier --check` gagal atas 17 berkas, dengan `.gitattributes` exit 0. Bentuk ADR-0020 diverifikasi ulang independen: tanpa `--workspace` cargo menjalankan 2 unittest + 1 doc-test, dengan `--workspace` 3 + 2. **Test baru `tests/unit/toolchain-file-coverage.test.js`** mengunci kesamaan himpunan ejaan antar `eslint.config.js` / `tsconfig.json` / `vitest.config.ts` — dijalankan terhadap HEAD ia gagal pada tiga hitungan, jadi load-bearing. **Satu temuan baru tester:** `SEGMENT_MATCH_CAVEAT` menyarankan "narrow `MAIN_IMPORT_GROUPS`", tetapi kalimat itu menempel pada dua pesan yang dipakai **dua** rule — ketika pesannya datang dari `no-restricted-syntax`, konstanta yang benar adalah `DYNAMIC_MAIN_IMPORT_SELECTORS`, sehingga pembaca akan menyempitkan konstanta yang salah. Nol dampak perilaku, tetapi kelas yang sama dengan W1. Siklus 7 (terakhir): koreksi kalimat itu + klaim JSDoc alias yang kini terbukti redundan. |
| 2026-08-09 11:14 | SETUP-03 | `in-progress` | **`done`** | **7 siklus. H2 terpenuhi: tester LULUS 145 test, auditor 0 Critical, seluruh Warning diperbaiki atau ditunda lewat entri `decisions.md`.** Siklus 7 menutup dua klaim terakhir. `SEGMENT_MATCH_CAVEAT` dibuat generik terhadap rule ("naming the rule that fired") alih-alih dipecah per rule — implementer beralasan sumbu divergensi yang konstanta bersama ini cegah adalah *pesan* (output vs shared), sementara memecah per *rule* akan melipatgandakannya jadi empat kombinasi dan menghidupkan lagi risiko itu; diukur enam arah, menamai rule yang salah memang tidak membungkam. Dua entri alias `@/main` **dihapus** setelah percobaan sendiri atas sepuluh specifier mengonfirmasi redundansi tanpa syarat (termasuk `~/main/x`, yang menunjukkan cakupan alias tidak pernah bergantung prefiks terdaftar); jaring pengamannya fixture `'aliased import'` milik tester. Satu jebakan dilaporkan yang menjelaskan mengapa komentar lama tidak pernah mengutip polanya verbatim: menulis glob ber-`**` di dalam blok JSDoc **menutup komentar itu** dan membuat `eslint .` gagal `SyntaxError`. **Verifikasi penutup oleh coordinator pada working tree final:** `npm test` 145/145 · `npm run lint` + `postlint` bersih · `vue-tsc` exit 0 · `npm run build` + `postbuild` 2 dokumen bersih · `cargo fmt --all --check` exit 0 · `cargo clippy -D warnings` exit 0 · `cargo test --workspace` exit 0 dengan `aeroworship_core` terlihat terjangkau. **Berikutnya: SETUP-06**, lalu SETUP-04. SETUP-07 menunggu pengguna dan tidak dapat dikerjakan agent mana pun. |
| 2026-08-09 11:20 | SETUP-06 | `todo` | `in-progress` | Didelegasikan ke implementer. Dipilih sesuai urutan yang sudah ditetapkan sejak 2026-08-08: sebelum SETUP-04, karena begitu SETUP-04 membuat database, database dev berisi lirik dan isi ibadah nyata — jauh lebih murah dikerjakan sebelum berkasnya ada. **Ketegangan desain yang ditandai coordinator di muka, bukan diserahkan ke tester:** daftar pola di judul item ini memuat `*.aero` dan `*.aerotpl`, sementara NFR-15 menuntut "unit tests with hostile path fixtures" dan NFR-28 menuntut "fuzzing corpus of 10,000 mutated files" — keduanya berkas `.aero`. Pola telanjang akan **membuat fixture keamanan tak dapat di-commit, dan absennya senyap**. Kelas kegagalan yang sama dengan ADR-0020, kali ini di `.gitignore`. Implementer diminta menyelesaikannya dengan pengecualian ber-scope, bukan dengan menghapus polanya. Catatan kedua: data root sesungguhnya adalah `%APPDATA%\AeroWorship` (PRD §6.9), di luar repo — jadi yang dijaga item ini adalah artefak dev yang nyasar, dan alasan itu harus tertulis supaya orang berikutnya tidak mengira polanya salah alamat. |
| 2026-08-09 11:34 | SETUP-06 | `in-progress` | `in-progress` | **Implementer selesai; tester + auditor didelegasikan.** Lima blok baru, nol baris lama disentuh. Ketegangan fixture diselesaikan dengan penanda **nama direktori**, bukan lokasi — `!**/fixtures/**/*.aero` dan `.aerotpl`. Alasannya menjawab persis kekhawatiran saya: pengecualian ber-anchor lokasi (`tests/**/fixtures/**`) memindahkan kegagalan senyap alih-alih menghilangkannya, karena fixture yang benar di `src-tauri/crates/core/tests/fixtures/` (konvensi Cargo, sama sahnya dengan PRD §6.13) akan diam-diam terabaikan. Sempit pada **ekstensi**, luas pada **lokasi**: sengaja bukan `!**/fixtures/**`, sehingga `.env`, `*.db`, dan `*.log` tetap terabaikan bahkan di dalam `fixtures/` — diverifikasi dengan berkas sungguhan. **Koreksi di tengah jalan yang dilaporkan sendiri:** draf pertama memakai dua baris per ekstensi dengan komentar bahwa `fixtures/**/x` tidak cocok dengan berkas tepat di dalam `fixtures/`; verifikasi membuktikan itu salah — `/**/` cocok dengan nol direktori — lalu baris redundan dan komentarnya dihapus. `config.json` **sengaja tidak** diabaikan (pola telanjang seumum itu cocok di kedalaman berapa pun dan bertabrakan dengan config yang sah; isinya penugasan display, bukan data jemaat); `session.autosave.json` dimasukkan karena namanya unik dan isinya setara `.aero`. Cross-check `git ls-files` × `check-ignore` atas 53 berkas terlacak: nol pola terlalu lebar. `npm run lint` dan `npm test` (145) hijau. **Dilaporkan tidak dikerjakan:** isi `cache/`/`media/` yang nyasar belum tertutup — pola direktori bare-name berisiko menelan direktori sumber, jadi butuh keputusan tersendiri saat SETUP-04 atau item import benar-benar membuat direktori itu. |
| 2026-08-09 11:53 | SETUP-06 | `in-progress` | `in-progress` | **Siklus 1 diverifikasi: tester LULUS (157 test), auditor 0 Critical / 2 Warning / 3 Suggestion.** Kelima criterion C1–C5 dieksekusi atas 64 berkas nyata, bukan lewat `check-ignore` saja — `git add --dry-run` dipakai untuk membuktikan fixture benar-benar addable. Test permanen ditulis (`tests/unit/gitignore-guard.test.js`, 12 test, repo throwaway di `os.tmpdir()` dengan `core.excludesFile` dinetralkan) plus mutation check enam arah yang seluruhnya membalik verdict. **Tester membongkar satu bukti implementer yang hampa:** bentuk `git ls-files \| git check-ignore --stdin` **tidak pernah bisa** melaporkan berkas terlacak — tanpa `--no-index`, `check-ignore` membaca index dan melewati path yang sudah terlacak; dibuktikan di repo terisolasi dengan `keep.db` terlacak. Jadi "nol keluaran" itu nol menurut konstruksi, bukan menurut pola. Dijalankan ulang dengan `--no-index`: **C4 tetap lulus**, tetapi kelas kegagalannya persis ADR-0020, dan test permanennya kini memakai `--no-index` supaya cacat itu tidak bisa kembali. **Auditor membalikkan penilaian implementer atas W1:** keberatan "pola direktori akan menelan `src/assets/media/`" menunjuk direktori yang **tidak ada di repo maupun di PRD §6.13**; pola ber-anchor root (`/cache/`, `/media/`) menggugurkan keberatan itu tanpa kehilangan cakupan. Isi `cache/decks/` adalah render visual penuh khotbah nyata, seringkali lebih terbaca daripada `.aero` yang hanya memuat UUID; FR-705 bahkan **mengharuskan** uji relink dengan `.aero` dan folder media nyata berdampingan. Ditutup di siklus 2. **W2 tidak dapat ditutup di item ini sama sekali** — dibawa ke SETUP-05 lewat [ADR-0023](decisions.md#adr-0023). Pertanyaan `config.json` terjawab dan penilaian implementer bertahan: daftar 10 path sesi FR-302 ada di tabel `recent_sessions` SQLite (Appendix A), bukan di `config.json`, jadi sudah tertutup `*.db`. |
| 2026-08-09 15:50 | SETUP-06 | `in-progress` | `in-progress` | **Siklus 2: implementer mati oleh session limit tepat sesudah menulis polanya, sebelum memverifikasi atau melapor.** Substansinya utuh di disk — blok "Cache render dan media" dengan `/cache/` dan `/media/` ber-anchor root beserta alasannya; **verifikasi dijalankan coordinator sendiri** karena laporannya tidak pernah ada: ketiga path realistis (`cache/decks/abc-123/slide-0001.webp`, `cache/thumbs/x.webp`, `media/foto.jpg`) terabaikan oleh baris 117–118; kontrol negatif (`src/assets/media/logo.png`, `src/shared/cache/x.ts`, `src/main/media/icon.svg`) **nol** yang cocok — anchor bekerja seperti yang auditor perkirakan; cross-check berkas terlacak **dengan `--no-index`** nol; `npm test` 157; lint bersih. Sapuan `--ignored -uall` mengonfirmasi nol residu probe di luar `dist/`/`node_modules/`/`target/`/`.claude/`. **Yang hilang bersama laporannya:** keputusan atas `autosave/` dan `templates/` tidak pernah dinyatakan. Penilaian coordinator, untuk diperiksa tester dan auditor: `autosave/` tidak perlu karena `session.autosave.json` sudah tertutup namanya, dan `/templates/` **tidak boleh** ditambahkan sama sekali — FR-410 menyatakan `templates/builtin/` adalah template yang **dikapalkan bersama aplikasi**, jadi pola direktori di situ akan memblokir berkas yang justru harus masuk repo; `templates/user/*.aerotpl` sendiri sudah tertutup `*.aerotpl`. Tester dan auditor dijalankan untuk mensertifikasi siklus 2. |
| 2026-08-09 16:15 | SETUP-06 | `in-progress` | `in-progress` | **Auditor siklus 2: 0 Critical, 3 Warning, 2 Suggestion, penilaian "tutup — dengan syarat W2 diperbaiki lebih dulu". Tester putus koneksi di butir terakhir sesudah menulis test-nya; 162 test lulus (naik 5), nol residu.** Test barunya mencakup mutation check anchor ("would swallow the source tree if the anchors were removed") dan interaksi `fixtures/` di dalam direktori ber-nama cache/media. **W1:** anchor benar, cakupannya kurang — bentuk salinan-sebagai-direktori (`AeroWorship/cache/decks/…`), `tmp/data/`, dan bundel relink FR-705 di `tests/integration/fixtures/relink-a/media/` semuanya lolos, dan nol pola ekstensi menyentuh `.webp`. Auditor menilai ini batas prinsip `.gitignore`, bukan cacat dua baris itu; dilipat ke cakupan guard [ADR-0023](decisions.md#adr-0023) yang menilai **apa** yang di-stage, bukan **di mana**. **W2 memblokir `done`:** komentar `.gitignore:102-104` berbunyi "cakupannya penuh" — tidak benar menurut W1. Bukan cacat penjagaan melainkan klaim yang salah, di repo yang punya tiga ADR menolak persis itu. Satu kalimat; didelegasikan. **W3 — penalaran coordinator atas `/templates/` tidak berdiri:** PRD §6.13 tidak memuat `templates/` di akar repo dan §6.9 menempatkan `templates/builtin/` di dalam data root, di luar repo; Appendix A menyimpan template di tabel SQLite. Kesimpulan (jangan tambahkan `/templates/`) benar, premisnya salah, dan premis salah itu menyembunyikan cacat **aktif hari ini** di `.gitignore:70` — `*.aerotpl` sudah mengabaikan calon sumber template bawaan FR-410. Dicatat lengkap dengan koreksinya di [ADR-0024](decisions.md#adr-0024). Penilaian atas `autosave/` dikonfirmasi benar; sibling atomic-write (`session.autosave.json.tmp`) dibawa ke item FR-707. ADR-0023 diperbaiki atas S2 — cakupan guard kini menyebut kedua ekstensi yang di-unignore, bukan hanya `.aero`. |
| 2026-08-09 16:40 | SETUP-06 | `in-progress` | `in-progress` | **W2 diperbaiki: klaim "cakupannya penuh" dihapus.** Blok kini menyatakan batasnya (bentuk akar saja), satu contoh yang lolos (salinan-sebagai-direktori `AeroWorship/cache/…`), dan ke mana sisanya pergi ([ADR-0023](decisions.md#adr-0023)) beserta alasannya dalam satu klausa. Nol pola tersentuh — dikonfirmasi dua arah: `check-ignore --no-index` masih menunjuk baris `/cache/` dan `/media/` (nomor bergeser 117/118 → 120/121 karena komentar bertambah), kontrol negatif tetap nol keluaran, dan 162 test milik tester tetap hijau (ia membaca `.gitignore` asli, jadi itu jaring pengaman otomatis). **Implementer melaporkan tiga hal yang tidak ia penuhi alih-alih membulatkannya:** permintaan "lebih pendek kalau bisa" tidak terpenuhi (blok 19 → 22 baris; ia tidak menemukan cara memenuhi ketiga hal yang diminta tanpa menyerempet paragraf yang dilarang disentuh); tiga bentuk lolos lainnya tidak disebut namanya sehingga pembaca yang ingin daftar lengkap harus ke ADR-0023; dan tautan Markdown `[ADR-0023](…)` ditulis di berkas non-Markdown yang belum punya preseden tautan sama sekali — konvensi berkas itu sitasi telanjang (`PRD §6.9`, `FR-705`). Yang ketiga akibat brief coordinator yang menuliskannya persis begitu; dibiarkan, tidak sepadan satu siklus. |
| 2026-08-09 16:51 | SETUP-06 | `in-progress` | **`done`** | **3 siklus. H2 terpenuhi: tester LULUS 162 test, auditor 0 Critical, seluruh Warning diperbaiki atau ditunda lewat entri `decisions.md`.** Diverifikasi ulang coordinator pada working tree final: `npm test` 162 · `npm run lint` + `postlint` bersih · `vue-tsc` exit 0. **Tester menemukan cacat di berkasnya sendiri dan melaporkannya alih-alih membiarkan hijau:** putaran ini **tidak** dimulai hijau — dua test-nya timeout pada default 5 s, karena tiap case memanggil git dan empat di antaranya membangun repo throwaway (diukur: `git init`+`status` 1,5 s; case `untrackedUnder` 3,1–8,7 s; berkas yang sama selesai dalam 12,5 s dan 60,7 s pada dua run berurutan tanpa satu byte berubah). Laporan "162 lulus" sesi sebelumnya benar untuk run-nya; yang tidak dilaporkan adalah bahwa ia lulus **tanpa margin**. Diperbaiki dengan budget per-`describe` 60 s mengikuti preseden `eslint-bundle-boundary.test.js:57` — bukan `testTimeout` global, karena angka global dibelanjakan tiap test masa depan termasuk yang seharusnya gagal cepat. **Perbaikannya dibuktikan, bukan diasumsikan:** di bawah `--testTimeout=1` ketujuh belas case tetap lulus (nilai lokal menang atas flag) sementara kontrol `toolchain-file-coverage.test.js` mati kelima-limanya — tanpa kontrol itu, "17 lulus" bisa berarti flag-nya yang tidak berfungsi. Butir A terjawab: parser `verdicts()` memotong sesudah titik dua kedua dan membandingkan **teks pola**, bukan nomor baris, jadi pergeseran 117/118 → 120/121 tak terlihat olehnya — nol regresi. C1–C5 masih berdiri, keduabelas test siklus 1 utuh dan hijau. **Berikutnya: SETUP-04** (skema SQLite), lalu SETUP-05 yang memikul ADR-0023. SETUP-07 menunggu pengguna. |
| 2026-08-09 17:00 | SETUP-07 | `todo` | `in-progress` | **Diserahkan ke pengguna — satu-satunya item yang tidak dapat dikerjakan agent mana pun.** Dipilih mendahului SETUP-04 atas rekomendasi coordinator dan disetujui pengguna. Alasannya bukan urutan daftar: aplikasi belum pernah dijalankan sekali pun sejak commit pertama, sementara tiga item sudah ditutup di atasnya. Panic startup atau CSS yang gagal dimuat tidak akan muncul di gate mana pun — keenam perintah verifikasi memeriksa build dan test, bukan jendela yang benar-benar terbuka. SETUP-04 menambahkan lapisan database; bila dasar yang belum terverifikasi itu ternyata rusak, dua masalah akan bercampur. Hari ini permukaannya masih sekecil mungkin: satu jendela, satu label, satu warna latar. Installer yang diuji: `src-tauri/target/release/bundle/nsis/AeroWorship_0.1.0_x64-setup.exe`, 1.394.592 byte, dibangun 2026-08-08 11:22 — build rilis, **bukan** `tauri dev`, karena CSP tidak ditegakkan di mode dev ([ADR-0015](decisions.md#adr-0015)) sehingga hijau di sana tidak bermakna. Yang dikonfirmasi: jendela terbuka ~1280×800, teks "AeroWorship Control Panel" hadir, latar `#16181d` (bukan putih). Poin ketiga yang sesungguhnya diuji — latar putih atau teks tanpa gaya berarti CSS gagal dimuat, yaitu CSP menolak sesuatu. Devtools tidak tersedia di rilis, jadi render bergaya benar adalah satu-satunya sinyal yang ada. Menunggu laporan pengguna. |
| 2026-08-09 17:12 | SETUP-07 | `in-progress` | **`done`** | **LULUS — aplikasi berjalan untuk pertama kalinya sejak commit pertama, dikonfirmasi pengguna lewat screenshot build rilis.** Keempat butir terpenuhi: jendela terbuka tanpa panic, judul "AeroWorship" dengan ukuran sesuai, teks "AeroWorship Control Panel" hadir, dan **latar `#16181d` dengan teks bergaya** — heading besar-tebal, body ber-ukuran benar, font sans dan bukan serif bawaan. Butir terakhir itu yang sesungguhnya diuji: ia membuktikan stylesheet ter-link **benar-benar dimuat di bawah `style-src 'self'`** ([ADR-0015](decisions.md#adr-0015)). Kalau CSP menolaknya, yang tampil adalah teks hitam polos di latar putih. Assertion build-time `dist/*.html` kini punya konfirmasi runtime-nya, bukan hanya bukti tidak langsung. **Membuka [ADR-0015 S4](decisions.md#adr-0015)** (`freezePrototype`), yang selama ini menunggu baseline "aplikasi jalan". **Satu cacat ditemukan yang tidak ada di daftar periksa** — persis nilai yang hanya bisa diberikan verifikasi runtime: scrollbar vertikal muncul di jendela berisi dua baris teks. Didiagnosis coordinator: `src/main/App.vue:34-36` memakai `min-height: 100vh` bersama `padding: 2rem` dengan `box-sizing` bawaan `content-box`, sehingga tinggi totalnya 100vh + 4rem. **Proyektor tidak terpengaruh** — `src/output/Renderer.vue:34-40` memakai `height: 100vh` + `overflow: hidden` tanpa padding, dan komentarnya menyatakan scrollbar memang dijaga di sana. Ironinya patut dicatat: penulisnya sadar scrollbar adalah mode kegagalan, menjaga proyektornya, lalu memasukkannya kembali lewat padding di Control Panel. Bukan kegagalan SETUP-07 (keempat criterion-nya lulus); dicatat sebagai temuan dengan pemilik. |
| 2026-08-09 17:20 | — | — | — | **Tindak lanjut temuan SETUP-07: `box-sizing: border-box` global, didelegasikan ke implementer.** Pengguna memilih menetapkannya untuk seluruh frontend alih-alih menambal `.control-panel` saja. Alasan yang mendasari pilihan itu: CSS placeholder di `App.vue` akan diganti seluruhnya saat Control Panel sungguhan datang, jadi yang benar-benar bertahan dari perbaikan ini adalah keputusan `box-sizing`-nya — dan keputusan itu lebih murah diambil sekali sekarang daripada diubah setelah FR-1xx…FR-7xx menumpuk komponen yang mengasumsikan `content-box`. Bug yang baru ditemukan adalah contoh persis mengapa. |
| 2026-08-09 18:25 | — | — | — | **`box-sizing` global selesai — keputusan diambil dengan ukuran, bukan prinsip.** Implementer memilih satu berkas bersama (`src/shared/styles/base.css`) diimpor kedua entry point, dan mengukur harganya alih-alih memperdebatkannya: karena keduanya mengimpornya, Vite mengeluarkannya sebagai **satu CSS chunk bersama** yang di-*link* kedua dokumen — bukan disalin ke masing-masing entry. `output-*.css` **tidak berubah** di 288 B; biaya nyata ke bundle projector 40 B CSS + 96 B markup = **136 B**, dan selisih atas opsi duplikat hanya **97 byte** terhadap baris anggaran 28 MB PRD §5.1. Dengan selisih sebesar itu, argumen anti-drift `eslint.config.js` menang tanpa perlu ditimbang: dua salinan CSS yang menyimpang tidak memerahkan gate mana pun. `.control-panel` diperiksa sesudahnya dan tetap masuk akal — di bawah `border-box`, `min-height: 100vh` menjadikan 100vh tinggi kotak border sehingga padding digambar di dalamnya, dan margin `<h1>` tidak bisa collapse keluar karena induknya berpadding. Diverifikasi coordinator: `npm test` 162 · lint · typecheck · `build` + `postbuild` 2 dokumen bersih. **Duplikasi `html, body { margin: 0 }` di kedua SFC sengaja dibiarkan** — `background`-nya berbeda (#16181d vs #000) dan alasannya berbeda; menyatukannya item tersendiri. **Belum dikonfirmasi visual** — scrollbar hanya terlihat di aplikasi yang berjalan. Untuk cek ini `tauri dev` memadai dan tidak melanggar [ADR-0015](decisions.md#adr-0015): yang tidak ditegakkan di dev adalah CSP, sementara yang diperiksa di sini adalah layout. |
| 2026-08-09 18:40 | — | — | — | **Perbaikan `box-sizing` dikonfirmasi visual oleh pengguna; scrollbar hilang.** Satu putaran sempat tampak gagal karena pengguna memasang ulang installer 8 Agustus alih-alih menjalankan `tauri dev` — build itu mendahului perbaikannya. Sebelum menebak ulang, coordinator memeriksa dua fakta yang menyingkirkan hipotesis lain: `dist/assets/_plugin-vue_export-helper-*.css` benar memuat `*,:before,:after{box-sizing:border-box}`, dan ia **ter-link di `<head>` `index.html` maupun `output.html`** — jadi bukan kasus CSS chunk bersama yang gagal ditautkan Vite. **Pelajaran prosedural yang perlu dipegang seterusnya:** perubahan frontend diverifikasi lewat `tauri dev` (cepat, devtools tersedia di build debug), sementara perubahan CSP dan guard rilis hanya bermakna lewat installer rilis ([ADR-0015](decisions.md#adr-0015)). Menguji lewat installer lama membuat setiap perbaikan frontend tampak tidak bekerja. |
| 2026-08-09 18:40 | SETUP-04 | `todo` | `in-progress` | Didelegasikan ke implementer. Item terakhir Fase 0 yang dapat dikerjakan agent; SETUP-05 menyusul dan memikul [ADR-0023](decisions.md#adr-0023). **Empat jebakan ditandai coordinator di muka, bukan diserahkan ke tester:** (1) `PRAGMA foreign_keys`, `synchronous`, dan `cache_size` bersifat **per-koneksi** dan tidak tersimpan di berkas — hanya `journal_mode = WAL` yang persisten; PRAGMA yang dijalankan sekali saat migrasi akan tampak benar dan diam-diam mati di setiap koneksi berikutnya, kelas kegagalan yang sama dengan ADR-0020. (2) Trigger `trg_sections_ai` di Appendix A **tidak boleh diimplementasikan apa adanya** — catatan implementasi PRD sendiri menyatakan ia ilustratif dan harus diganti `reindex_song(song_id)` di dalam transaksi yang sama; nilai `'rebuild-song'` juga bukan perintah FTS5 yang sah. (3) `rusqlite` butuh feature `bundled` **dan** `fts5` terpisah; `bundled` saja tidak memuat FTS5, dan kegagalannya baru muncul saat `CREATE VIRTUAL TABLE` dijalankan. (4) `trg_songs_default_arrangement_fk` hanya `BEFORE UPDATE`, sehingga `INSERT` dengan `default_arrangement_id` yang salah lolos — dilaporkan sebagai pertanyaan ke pengguna sesuai H4, bukan diperbaiki diam-diam. |
| 2026-08-09 20:49 | SETUP-04 | `in-progress` | `in-progress` | **Implementer mati oleh session limit sebelum membaca apa pun; working tree diverifikasi bersih, nol residu.** Tidak dihitung sebagai kegagalan H3 — H3 menghitung siklus di mana tester melaporkan test gagal atau auditor melaporkan Critical, dan agent yang kehabisan kuota bukan salah satunya. Posisi SETUP-04 tetap nol kegagalan. Didelegasikan ulang dengan brief yang sama. |
| 2026-08-09 21:50 | SETUP-04 | `in-progress` | `in-progress` | **Implementer selesai; dua keputusan diangkat ke pengguna sebelum tester dijalankan.** Skema Appendix A lengkap sebagai migrasi 001 tunggal; runner memakai `include_str!` dengan urutan diperiksa **saat kompilasi** (`const _: () = assert!`); DDL sengaja tanpa `IF NOT EXISTS` supaya database yang pembukuannya bilang "belum" tapi tabelnya sudah ada gagal keras, bukan ditambal diam-diam. Dua pembukuan versi (`schema_migrations` + `PRAGMA user_version`) ditulis di transaksi yang sama; ketidaksamaan → `InconsistentVersion`, bukan ditebak. Versi lebih baru ditolak `FutureSchema` meniru sikap FR-708. Satu transaksi **per migrasi**, dan `journal_mode = WAL` sengaja tidak ada di SQL migrasi karena ia tidak bisa jalan di dalam transaksi. Diverifikasi coordinator: `cargo fmt --all`, `clippy -D warnings`, `cargo test --workspace` (5 target, 0 test) semuanya exit 0. **Empat jebakan yang saya tandai: tiga terbukti, satu klaim saya salah.** (1) PRAGMA per-koneksi terbukti dan lebih halus dari dugaan — `synchronous` dan `cache_size` benar-benar mati di koneksi mentah (NFR-11 dan plafon 8 MB NFR-01 hilang senyap), tetapi **`foreign_keys` kebetulan tetap `1`** karena `libsqlite3-sys` membangun dengan `-DSQLITE_DEFAULT_FOREIGN_KEYS=1`; implementer menyatakan sendiri bahwa bukti "FK menyala di koneksi baru" karena itu **tidak membuktikan kodenya bekerja**, dan hanya berhenti menyala bila `bundled` dilepas. (2) `trg_sections_ai` lebih buruk dari "ilustratif": `'rebuild-song'` membuat setiap INSERT ke `song_sections` **gagal** (`SQL logic error`); sengaja tidak dibuat, dicatat 34 baris komentar di kepala berkas migrasi. (3) **Klaim coordinator soal feature `fts5` keliru** — `rusqlite` 0.40 tidak punya feature itu dan memintanya adalah error resolver; FTS5 datang dari `libsqlite3-sys` yang mengompilasi dengan `-DSQLITE_ENABLE_FTS5`. Konsekuensinya penting: tidak ada flag yang menjaganya, jadi penjaga satu-satunya adalah migrasi 001 yang menjalankan `CREATE VIRTUAL TABLE` sehingga `migrate()` gagal keras bila hilang. (4) Celah `BEFORE INSERT` **terbukti empiris** — lihat baris berikutnya. **NFR-16 diukur:** biner 5.132.800 → 6.260.736 (+22,0%), installer 1.394.472 → 1.942.471 (+39,3%) = **12,9% dari anggaran 15 MB**. Angka sebelum cocok byte-per-byte dengan baseline ADR-0017. |
| 2026-08-09 22:05 | SETUP-04 | `in-progress` | `in-progress` | **Dua keputusan pengguna diambil; keduanya memilih memperbaiki sumbernya.** (1) Data root tetap `%APPDATA%\AeroWorship` sesuai PRD §6.9 literal, bukan `app_data_dir()` Tauri — dicatat [ADR-0025](decisions.md#adr-0025) beserta koreksi atas catatan konsekuensi ADR-0010, dan beserta jebakan yang ditimbulkannya: setiap plugin masa depan yang memanggil `app_data_dir()` akan menunjuk direktori berbeda yang hidup berdampingan tanpa gejala. (2) Celah `BEFORE INSERT` ditutup **dengan memperbaiki PRD Appendix A**, bukan hanya skemanya — [ADR-0026](decisions.md#adr-0026). Konsekuensi yang perlu dipahami dan bukan sekadar diterima: sesudah trigger itu ada, `INSERT INTO songs` dengan `default_arrangement_id` non-NULL akan **selalu** ditolak, dan itu satu-satunya urutan yang mungkin karena `song_arrangements.song_id` mereferensikan `songs(id)` — alur sah selalu tiga langkah (INSERT NULL → buat arrangement → UPDATE), dan trigger menjadikannya ditegakkan skema alih-alih konvensi yang harus diingat. Karena PRD ikut diperbaiki, siklus 2 juga menimbang **mengamandemen migrasi 001 alih-alih menambah 002**, supaya properti "001 ≡ Appendix A" — yang implementer sebut sebagai alasan menyimpan SQL sebagai SQL agar bisa di-diff langsung — tidak pecah di perubahan pertama. Aman hanya bila nol database di luar sana sudah menerapkan 001. |
| 2026-08-09 22:30 | SETUP-04 | `in-progress` | `in-progress` | **Siklus 2 selesai; tester + auditor didelegasikan.** PRD Appendix A diperbaiki (izin eksplisit pengguna atas H4, hanya bagian itu) dan trigger yang sama diterapkan ke skema. **Implementer memverifikasi premis sebelum memutuskan amandemen 001**, seperti diminta: `%APPDATA%\AeroWorship` **tidak ada**, nol `aeroworship.db` di mana pun, dan `001_initial_schema.sql` masih `??` untracked — belum pernah meninggalkan working tree, jadi tidak ada mesin lain yang bisa memilikinya. Populasi yang sudah menerapkan 001 nol, bukan hanya di mesin ini. Ia juga menyatakan risikonya apa adanya alih-alih membulatkannya: jalur "database ber-`user_version=1` memakai skema lama tanpa gejala" **memang ada dan senyap** karena `migrate()` memfilter `version > current`; yang menutupnya bukan kode melainkan populasi kosong yang terverifikasi — *"jaminan yang hanya berlaku hari ini, dan hari ini satu-satunya hari ia gratis."* **Bukti hidup ADR-0025 ditemukan di mesin pengguna:** `%LOCALAPPDATA%\AeroWorship` (installer) dan `%LOCALAPPDATA%\id.aeroworship.app` (EBWebView) sudah berdampingan — dua root bernama beda, persis jebakan yang ADR itu catat. **Trigger terbukti dua arah:** INSERT dengan arrangement milik lagu lain kini **ditolak** (`ConstraintViolation 1811`, siklus 1 menerimanya); INSERT dengan arrangement milik dirinya sendiri **juga ditolak**; dan pra-membuat arrangement sebelum lagunya ada gagal `FOREIGN KEY 787` — itu yang membuat penolakan kedua tak terhindarkan, dibuktikan bukan disimpulkan. Alur sah tiga langkah bekerja; trigger UPDATE lama nol regresi; jumlah trigger 1 → 2; `integrity_check` ok dua kali. **Implementer menolak membulatkan satu hal lagi:** penjaga `app_data_dir()` yang ia tambahkan **hanyalah komentar**, dan ia menyebut sendiri bahwa repo ini menolak konvensi tanpa penegakan di ADR-0013/0016/0020 — kandidat test grep, diteruskan ke tester sebagai keputusan. |
| 2026-08-12 13:06 | SETUP-04 | `in-progress` | `in-progress` | **Tester + auditor siklus 2 mati oleh limit mingguan sebelum menghasilkan laporan; didelegasikan ulang setelah kuota reset.** Tester sempat mengeluarkan satu baris (`"PATH repairs fine when set explicitly. Now let me survey the repo."`), auditor nol keluaran. **Tidak dihitung sebagai kegagalan H3** — H3 menghitung siklus di mana tester melaporkan test gagal atau auditor melaporkan Critical, dan agent yang kehabisan kuota bukan salah satunya; sama seperti enam kematian agent sebelumnya di sesi ini. Posisi SETUP-04 tetap nol kegagalan. Jeda ~63 jam (9 Agu 22:30 → 12 Agu 13:06, reset kuota 13:00 WIB). **Working tree diverifikasi coordinator tidak bergeser satu byte pun selama jeda:** tujuh berkas `M` dan lima `??` yang sama, nol residu harness `setup04_probe.rs` (nol `examples/`, nol `src/bin/`). Gate Rust yang saya jalankan sendiri sebelum jeda tetap berlaku pada tree yang sama: `cargo fmt --all --check`, `clippy -D warnings`, `cargo test --workspace` (5 target, 0 test) semuanya exit 0, `npm test` 162 lulus. **H2 belum terpenuhi** — nol laporan tester, nol laporan auditor — jadi item tetap `in-progress`, dan itu satu-satunya alasan ia belum ditutup. |
| 2026-08-12 13:20 | SETUP-04 | `in-progress` | `in-progress` | **Auditor siklus 2 selesai: 0 Critical, 3 Warning, 5 Suggestion.** Tester masih berjalan, jadi H2 belum terpenuhi. **Auditor menguji premis brief alih-alih menerimanya, dan itu berbuah:** poin 8 (penerimaan `journal_mode = memory` di `verify_pragmas`) dibuktikan **benar** bukan lewat penalaran melainkan lewat membaca sumber C SQLite yang di-vendor — `sqlite3PagerSetJournalMode` di `libsqlite3-sys-0.38.2/sqlite3/sqlite3.c` hanya berpindah ke `PAGER_JOURNALMODE_MEMORY` bila pemanggil memang memintanya, dan kode kita tidak pernah meminta; poin 9 (`-DSQLITE_DEFAULT_FOREIGN_KEYS=1`) dikonfirmasi di `build.rs:153`, unconditional pada build `bundled`. **Warning:** (W1) `db::init` yang gagal menjalar ke `.expect()` di `run()`, dan pada rilis `windows_subsystem = "windows"` melepas console — jadi kegagalan startup **tidak punya permukaan diagnostik apa pun**: tidak ada jendela, tidak ada dialog, tidak ada log, proses sekadar menghilang. Skenario nyatanya persis alur yang NFR-30 dorong: relawan menyalin `aeroworship.db` tanpa `-wal`/`-shm` lalu memulihkannya. (W2) penjaga dua-data-root ADR-0025 hanya komentar — nol pemanggilan `app_data_dir()` lain hari ini (di-grep), jadi nol pelanggaran aktif, tetapi juga nol penegakan. (W3) nol test untuk seluruh lapisan database — sedang dikerjakan tester. **Suggestion yang paling berbobot: (S1)** `Connection::open` memakai `OpenFlags::default()` yang **menyalakan `SQLITE_OPEN_URI`**; hari ini tak berbahaya karena path selalu konstanta, tetapi `open()` dideklarasikan sebagai satu-satunya konstruktor untuk seluruh jalur masa depan, sehingga `?mode=ro`/`?vfs=…` menjadi sintaks hidup tanpa ada yang menyadarinya. **(S2) koreksi atas komentar implementer sendiri:** klaim penghematan "wasm FFI provider" di `Cargo.toml` tidak berlaku untuk target native — `sqlite-wasm-rs` sudah di-gate `cfg(target_family = "wasm")` dan tak pernah tercompile di sini; penghematan `cache`/`hashlink` terbukti nyata (absen di `Cargo.lock`), yang lain tidak. Kelas kegagalan berulang proyek ini lagi: komentar yang mengklaim lebih dari yang sesungguhnya. (S3) nol checksum yang mencegah migrasi yang sudah dikapalkan diedit diam-diam; (S4) batas isolasi berkas adalah akun OS, dan PRD memang **tidak** menuntut enkripsi at-rest (di-grep, bukan diasumsikan); (S5) kolom path relatif sengaja tanpa `CHECK` — diteruskan ke FR-5xx supaya keberadaan kolom tidak disalahartikan sebagai keamanan yang sudah ditangani. **Bersih dan dinyatakan eksplisit:** nol secret, nol injeksi SQL (seluruhnya `&'static str` atau parameter terikat), nol `unwrap`/`expect` baru di jalur input, nol command Tauri baru, manifest kapabilitas tak tersentuh, keempat crate baru berlisensi MIT dengan SQLite 3.53.2. **Tidak dapat dijalankan auditor:** `cargo audit` (tanpa `Bash`) — diserahkan ke coordinator. |
| 2026-08-12 14:00 | SETUP-04 | `in-progress` | `in-progress` | **Tester LULUS; siklus 3 dibuka untuk tiga perbaikan berbatas, bukan karena tester gagal.** `aeroworship-core` naik dari **nol `#[test]` menjadi 19** (11 unit + 8 integration) plus 4 test JS; nol gagal pada percobaan pertama, nol assertion dilonggarkan. **Dua dari tiga Warning auditor gugur karena pekerjaan tester ini, bukan karena ditunda:** W3 (nol test untuk lapisan database) ditutup 19 test; W2 (penjaga ADR-0025 hanya prosa) ditutup `tests/unit/app-data-dir-guard.test.js` — grep atas pohon `.rs` sungguhan yang membedakan **panggilan** `app_data_dir(` dari **sebutan** di komentar (yang `db.rs` lakukan tiga kali justru untuk melarangnya), lengkap dengan dua kontrol negatif sehingga "nol pelanggaran" tidak bisa berarti "regex-nya vakum". **Hipotesis P3 saya terbukti benar, dan ini kali pertama premis brief saya benar sesudah dua kali keliru di item ini:** doc comment `table_exists` mengklaim view terhitung `type = 'table'` di `sqlite_master`; diukur langsung, `CREATE VIEW` menghasilkan `type = 'view'`. Fungsinya sendiri benar — komentarnyalah yang salah, persis kelas kegagalan berulang proyek ini. **Tester menolak membulatkan tiga hal:** (a) hanya **jumlah** 33 tabel/8 indeks/2 trigger yang dikunci plus 4 anchor nama, karena enumerasi penuh akan menduplikasi SQL 001 tanpa menambah perlindungan; (b) `synchronous`/`cache_size` pada koneksi mentah dikunci sebagai `assert_ne!` terhadap nilai Appendix A, **bukan** sebagai nilai default pasti, karena default SQLite tidak pernah dinyatakan siapa pun sebagai kontrak; (c) GATE-G10 (cakupan ≥80%) **tidak diukur** — nol tool coverage terpasang. Ia juga menjelaskan angka 33 sampai ke akarnya, yang implementer tidak pernah rinci: tiap tabel FTS5 non-external-content menyumbang **6** entri `type='table'` (dirinya + 5 shadow), jadi 21 + 2×6 = 33 — dan ia mengoreksi draf komentarnya sendiri dari 4 ke 6 sebelum final. Kode error 1811 dan 787 diukur ulang, cocok. **Diverifikasi ulang coordinator pada working tree final:** `cargo fmt --all --check` exit 0 · `clippy --workspace --all-targets -D warnings` exit 0 · `cargo test --workspace` 19/19 · `npm run lint` + `postlint` bersih · `vue-tsc` exit 0 · `npm test` 166/166. **Lubang runtime yang ditandai tester dan dikonfirmasi coordinator di disk:** `%APPDATA%\AeroWorship` **tidak ada**, nol `aeroworship.db` di mana pun, dan yang terpasang di `%LOCALAPPDATA%\AeroWorship` adalah `aeroworship.exe` 5.132.800 byte tertanggal 8 Agu — biner **pra-rusqlite** milik SETUP-07. Klaim siklus 1 bahwa biner rilis membentuk data root karena itu tidak punya jejak tersisa, dan jalur `db::init` lewat `AppHandle` sungguhan belum terverifikasi pada kode hari ini. Diverifikasi coordinator **sesudah** siklus 3, supaya yang diuji adalah tree final. Konsekuensi yang perlu disadari sebelum dijalankan: begitu satu database menerapkan 001, premis "populasi nol" yang membenarkan **mengamandemen** 001 alih-alih menambah 002 berakhir — 001 menjadi final. Siklus 3 sengaja tidak menyentuh SQL supaya urutan ini aman. |
| 2026-08-12 14:25 | SETUP-04 | `in-progress` | `in-progress` | **Siklus 3 selesai, enam gate hijau; verifikasi runtime GAGAL DIJALANKAN COORDINATOR karena batas lingkungan, bukan karena cacat kode.** Tiga perbaikan tertutup, seluruhnya dengan mekanisme alih-alih komentar: (1) doc comment `table_exists` kini cocok dengan apa yang test buktikan — virtual table `type='table'` (sebab itulah `songs_fts`/`verses_fts` masuk hitungan 33), view `type='view'`; (2) komentar `Cargo.toml` **menjatuhkan separuh klaimnya sendiri** — penghematan `cache`/`hashlink` dipertahankan karena terverifikasi absen di `Cargo.lock`, klaim wasm diganti penjelasan bahwa `sqlite-wasm-rs` di-gate `cfg(target_family = "wasm")` di upstream sehingga tak pernah tertarik di build native; (3) **S1 ditutup dengan flag eksplisit**, `Connection::open_with_flags(path, READ_WRITE|CREATE|NO_MUTEX)` tanpa `URI`, plus doc comment yang menyatakan larangan mengembalikannya ke `Connection::open` tanpa membaca alasannya. **Implementer mengukur sebelum memutuskan, seperti diminta:** ia menulis probe sementara yang membuktikan janji doc `open()` soal `":memory:"` dan path kosong **tidak** bergantung pada flag URI, menjalankannya sampai lulus, lalu menghapusnya sebelum menyerahkan — nol residu di diff akhir; dan ia membaca sendiri `rusqlite-0.40.2/src/lib.rs:1256-1265` alih-alih memercayai kutipan saya. **Diverifikasi ulang coordinator:** `cargo fmt --all --check` exit 0 · `clippy --workspace --all-targets -D warnings` nol warning · `cargo test --workspace` 19/19 · `npm run lint` + `postlint` bersih · `vue-tsc` exit 0 · `npm test` 166/166. **Verifikasi runtime dicoba dan gagal karena lingkungan:** `npm run tauri dev` membangun bersih (vite siap, `cargo run` selesai 21,28 s) lalu `target\debug\aeroworship.exe` mati dengan **`exit code 0xc0000142, STATUS_DLL_INIT_FAILED`** — galat loader Windows yang terjadi **sebelum entry point**, khas proses GUI yang diluncurkan dari sesi non-interaktif tanpa window station. Bukan cacat item ini: nol kode kita yang berjalan. Konsekuensinya `db::init` lewat `AppHandle` sungguhan **tetap belum terverifikasi**, dan itu kelas yang sama dengan SETUP-07 — hanya pengguna yang dapat menjalankannya. `%APPDATA%\AeroWorship` masih tidak ada, jadi premis "populasi nol" ADR-0028 **belum berakhir** dan 001 masih boleh diamandemen sampai run pertama yang sungguhan. Vite yatim di port 1420 dimatikan; `git status` bersih, nol artefak `*.db`/`-wal`/`-shm`. **Sisa untuk menutup item:** satu run oleh pengguna. |
| 2026-08-12 15:11 | SETUP-04 | `in-progress` | **`done`** | **3 siklus. H2 terpenuhi: tester LULUS 19 test Rust + 166 test JS, auditor 0 Critical, ketiga Warning diperbaiki atau ditunda lewat entri `decisions.md`. Verifikasi runtime oleh pengguna LULUS.** Pengguna menjalankan `npm run tauri dev`; jendela terbuka dan bertahan, dan `%APPDATA%\AeroWorship\aeroworship.db` terbentuk — 274.432 byte. **Coordinator memeriksa isinya langsung dari header berkas, bukan menerima laporan,** karena nol `sqlite3` CLI di mesin ini: `magic = SQLite format 3`; **`write_ver`/`read_ver` di offset 18/19 keduanya `2`**, tanda WAL yang **tertulis di header berkas** dan karena itu satu-satunya bukti sungguhan bahwa `journal_mode = WAL` berlaku pada berkas nyata — tiga pragma lain per-koneksi dan tidak meninggalkan jejak di sini; `user_version = 1` di offset 60; `page_size = 4096`. Ketiadaan `-wal`/`-shm` justru benar: SQLite menghapusnya pada penutupan bersih, jadi itu tanda shutdown yang rapi, bukan WAL yang gagal. **Isi skema dicocokkan sampai angkanya:** 8 indeks dengan nama persis yang dikunci tester; 2 trigger termasuk `trg_songs_default_arrangement_fk_insert`, sehingga terbukti **migrasi hasil amandemen ADR-0026 yang mendarat**, bukan versi lama; 2 virtual table `songs_fts`/`verses_fts`, artinya FTS5 hidup di runtime — dan itu satu-satunya penjaganya, karena tidak ada feature flag yang menjaganya; 21 tabel biasa + 2×6 entri FTS (dirinya + 5 shadow) = **33**, persis rincian yang tester turunkan sendiri dan yang implementer tidak pernah rinci. **ADR-0028 aktif sejak run ini:** premis "populasi nol" berakhir, migrasi 001 **final**, perubahan skema berikutnya wajib migrasi 002. **Berikutnya: SETUP-05**, yang memikul [ADR-0023](decisions.md#adr-0023). Fase 0 tersisa satu item. |
| 2026-08-12 15:30 | SETUP-05 | `todo` | `in-progress` | **Didelegasikan ke implementer, siklus 1 dari dua yang direncanakan.** Item terakhir Fase 0 dan yang terpadat: pembangkitan tipe Rust→TS (NFR-33), kerangka `tests/{unit,integration,perf}`, guard [ADR-0023](decisions.md#adr-0023) dari SETUP-06, dan jalan keluar [ADR-0019](decisions.md#adr-0019). **Dipecah dua siklus atas alasan urutan, bukan kenyamanan:** siklus 1 mengerjakan kerangka `tests/` + guard staged-content + perintah perf, karena ADR-0023 menuntut guard ada **sebelum** fixture pertama ditulis dan item inilah yang membuat direktorinya; siklus 2 mengerjakan pembangkitan tipe, yang sepenuhnya orthogonal. Menggabungkannya berarti satu agent yang mati membawa keduanya. **Premis diperiksa coordinator di muka, dan dua di antaranya mengubah brief:** (a) `vitest.config.ts` **sudah** menjaring `tests/**` dengan delapan ejaan — catatan terbuka SETUP-03 "`tests/**` tidak terjaring" ternyata sudah tertutup saat SETUP-03 sendiri, jadi tidak diteruskan sebagai pekerjaan; (b) `src/shared/bindings/` **ada dan kosong**, `src-tauri/crates/core/src/models/mod.rs` kosong, dan nol command Tauri diperkenalkan sampai hari ini — dikonfirmasi auditor SETUP-04. **Jebakan ditandai coordinator di muka:** (1) guard dengan nol input akan hijau karena tidak menemukan apa-apa — `tests/` hari ini nol `fixtures/`, jadi ini kelas ADR-0020 yang paling gampang lahir; presedennya sudah ada di repo dan wajib ditiru, yaitu syarat exit-0 `check-dist-html.js` yang menuntut `reports.length > 0`. (2) ADR-0023 menuntut deteksi **positif** — fixture dibuktikan sintetis, bukan sekadar tidak terbukti nyata; heuristik "tidak terlihat seperti lirik" adalah deteksi negatif dan gagal justru pada kasus yang paling berbahaya. (3) Ekstensi **keduanya** (`*.aero` dan `*.aerotpl`), dan artefak render/media **terlepas dari path** — ADR-0023 menulis kalimat itu justru karena implementer yang membacanya sepintas akan membangun guard yang hanya memeriksa `*.aero` di satu lokasi. (4) Lifecycle-nya keputusan sungguhan, bukan detail: `.gitignore` guard hidup di `npm test`, tetapi pemeriksa **staged**-content secara alami membaca `git diff --cached` dan repo ini nol infrastruktur git hook — implementer diminta memutuskan dan beralasan, bukan menyalin bentuk yang salah. |
| 2026-08-12 15:40 | SETUP-05 | `in-progress` | `in-progress` | **Implementer siklus 1 mati oleh limit sesi (reset 18:00 WIB) di tahap survei, sebelum menulis satu berkas pun.** Keluaran terakhirnya masih berupa rencana membaca `src/assets`, `vitest.config.ts`, dan `eslint.config.js`. **Working tree diverifikasi coordinator: nol residu** — `tests/` tetap hanya `unit/` dengan 4 berkas, `scripts/` tetap 4 berkas, satu-satunya perubahan adalah `PROGRESS.md` milik coordinator sendiri. **Tidak dihitung sebagai kegagalan H3** — H3 menghitung siklus di mana tester melaporkan test gagal atau auditor melaporkan Critical; agent yang kehabisan kuota bukan salah satunya. Ini kematian agent ketujuh di sesi ini dan diperlakukan sama seperti enam sebelumnya. Posisi SETUP-05 tetap nol kegagalan. Brief siklus 1 disimpan utuh untuk delegasi ulang; nol perubahan diperlukan padanya, karena agent belum sempat melaporkan apa pun yang membantah premisnya. |
| 2026-08-12 21:50 | SETUP-05 | `in-progress` | `in-progress` | **Siklus 1 selesai; tester + auditor didelegasikan.** Tiga pekerjaan tertutup. `tests/integration/` dan `tests/perf/` lahir dengan `.gitkeep` bermakna mengikuti konvensi repo yang **sudah ada** (`src/main/stores/.gitkeep` dkk.), bukan pola baru. ADR-0019 ditetapkan di satu tempat lewat `test:perf:shell` / `test:perf:core`, dan implementer menjalankan `test:perf:shell` **sungguhan** — compile + run, exit 0 — yang memverifikasi lebih jauh daripada ADR-0019 sendiri, karena ADR itu hanya pernah membuktikan varian `--no-run`. **Guard ADR-0023 mengambil tiga keputusan desain yang saya serahkan terbuka, dan ketiganya beralasan:** (1) **deteksi positif** diwujudkan lewat sidecar `fixtures.manifest.json` yang menuntut `synthetic: true` literal + `purpose` ≥20 karakter, dan implementer **menolak** bentuk "marker di dalam JSON `.aero`" dengan alasan yang benar — korpus NFR-28 adalah berkas **termutasi** yang bisa jadi bukan JSON valid sama sekali, sehingga marker in-band tidak akan terbaca justru pada berkas paling berbahaya; (2) **lifecycle memindai index git** (`git ls-files` + `cat-file --batch`) lewat `pretest`, bukan `git diff --cached`, karena hanya bentuk itu yang melihat lubang ketiga ADR-0023 — berkas yang **terlanjur terlacak** tidak pernah muncul di diff staged; konsekuensinya dinyatakan jujur alih-alih dibulatkan: guard **tidak** memblokir `git commit` secara sinkron, ia terpicu di `npm test` berikutnya; (3) **cakupan media dinilai dari isi, bukan lokasi maupun ekstensi** — sniffing magic-byte atas seluruh index dengan satu allowlist `src-tauri/icons/`, dibuktikan dengan `stray-photo.png` di akar repo yang ditolak murni oleh isinya. **Guard dibuktikan menolak, bukan hanya lolos** — enam skenario dijalankan sungguhan lalu dibersihkan, termasuk `.webp` tanpa manifest, `.aero` di luar `fixtures/`, dan manifest ber-`synthetic: false`; lalu lolos (`2 candidate(s) verified synthetic`) sesudah manifest lengkap. **Dua premis dikoreksi implementer dan keduanya saya verifikasi sendiri:** (a) ADR-0023 menyatakan `src/assets/media/` "ada di pohon kerja hari ini" sebagai alasan pola berbasis-lokasi berbahaya — **direktori itu tidak ada**; `git ls-files src/` hanya mengembalikan 12 berkas dan nol di antaranya di bawah `src/assets/`. Kesimpulan ADR tetap berdiri, premisnya tidak. Implementer sengaja **tidak** menambahkannya ke allowlist atas dasar premis yang salah, dan menyerahkan keputusan itu ke item yang kelak membuat direktorinya — penilaian yang benar. (b) Brief saya menyiratkan pesan `compile_error!` masih hanya menyarankan `npm run tauri build`; dibaca ulang, `src-tauri/src/lib.rs:54-55` **sudah** menyebut perintah ADR-0019 lengkap. Premis saya salah, dan ia melaporkannya alih-alih menyentuh berkas itu tanpa perlu. **Diverifikasi ulang coordinator:** `cargo fmt --all --check` exit 0 · `clippy --workspace --all-targets -D warnings` nol warning · `cargo test --workspace` 19/19 · `npm run lint` + `postlint` bersih · `vue-tsc` exit 0 · `npm test` **203** (166 → 203, +37 test guard), dengan `pretest` ikut jalan dan melaporkan "0 candidate(s) found" pada tree bersih. |
| 2026-08-12 22:05 | SETUP-05 | `in-progress` | `in-progress` | **Auditor siklus 1: 1 Critical, 4 Warning, 3 Suggestion. H2 memblokir penutupan, jadi siklus 2 akan menutup Critical dulu; pembangkitan tipe Rust→TS bergeser ke siklus 3.** Tester masih berjalan. **[C1] — diverifikasi ulang coordinator langsung di kode, bukan diterima:** `buildBatchRequest` (`fixture-content-guard.js:242-244`) menyusun `` `:${path}\n` `` **tanpa memvalidasi bahwa `path` sendiri bebas dari `\n`**, sementara git mengizinkan newline di dalam nama path (satu-satunya byte terlarang `/` dan NUL). `check-fixture-content.js:101` benar memakai `git ls-files -z` untuk menjaga jaminan itu — lalu jaminannya dibuang saat path dikirim balik ke `cat-file --batch` lewat stdin yang **berbasis baris**. Satu path ber-newline karena itu pecah menjadi dua baris permintaan, sedangkan `parseCatFileBatch` mengonsumsi **murni berdasarkan posisi** — doc comment-nya menyatakan sendiri "entries can be read positionally against `keys` without git ever telling us which path an entry belongs to". Akibatnya guard membaca konten berkas **lain** sebagai konten path itu, tidak menemukan magic-byte media, dan melaporkan exit 0. **Guard melapor bersih atas byte yang tidak pernah ia baca** — kelas kegagalan yang sama dengan seluruh sejarah ADR-0020 proyek ini, kali ini di guard yang justru dibangun untuk menahan dokumen jemaat. Auditor menyusun konstruksinya langkah demi langkah lewat `git hash-object -w --stdin` + `update-index --add --cacheinfo`, bukan sekadar menduga. **Penilaian coordinator atas severity:** jalur eksploitasinya menuntut penguasaan git plumbing dan tidak lahir dari `git add` biasa, jadi Critical terasa tinggi untuk model ancaman yang sesungguhnya (kebocoran **tak sengaja**) — tetapi perbaikannya sekitar tiga baris dan arahnya sama dengan seluruh sisa guard ini, yaitu fail-closed, sehingga memperdebatkan severity lebih mahal daripada menutupnya. Ditutup di siklus 2, bukan ditunda. **[W2] diverifikasi juga:** komentar `fixture-content-guard.js:176-181` merujuk `check-fixture-content.js` sebagai tempat gap PPTX/PDF "dinamai sebagai accepted gap"; di-grep, **nol kata** tentang PPTX, PDF, atau accepted gap di berkas itu. Klaim komentar tidak benar — kelas yang sama yang sudah dua kali mahal di ADR-0020/0021. **[W1] paling layak perhatian dan bukan cacat implementasi:** deteksi kandidat hanya lewat ekstensi terdaftar atau magic-byte biner, sehingga `.aero` sungguhan yang **di-rename** jadi `notes.json`/`service.txt`/tanpa ekstensi tidak pernah menjadi kandidat — isinya JSON teks, nol signature cocok. Ini secara literal **di luar** cakupan ADR-0023 (yang membatasi diri pada dua ekstensi + media biner) dan bukan regresi, tetapi celahnya jatuh persis pada aset paling berisiko yang ADR itu sendiri sebut: `.aero` yang menyematkan lirik penuh (FR-706). Butuh keputusan cakupan yang tercatat, bukan keheningan. **[W3]** submodule di bawah `fixtures/` opak — `git ls-files` tanpa `--recurse-submodules` hanya melihat satu entri gitlink. **[W4]** test allowlist hanya menegaskan keanggotaan (`toContain`), sehingga menambah prefix selebar `src-tauri/` telanjang tidak menggagalkan satu test pun — persis cacat yang ADR-0020/0021 buktikan mahal. **Diperiksa auditor dan dinyatakan bersih:** ketiga lubang inti ADR-0023 memang tertutup oleh satu mekanisme yang sama (`git ls-files`, bukan `diff --cached`); nol argument injection (`execFileSync` argv literal, `shell` tidak diset, nol path pernah masuk argv); **fail-closed konsisten** di seluruh jalur error yang diperiksa (git hilang, bukan repo, `cat-file` gagal, manifest rusak) — C1 kelas berbeda, yaitu misatribusi, bukan kegagalan perkakas; allowlist ber-trailing-slash memang mencegah `src-tauri/icons-evil/`; `test:perf:shell` menyalakan `custom-protocol` ke arah yang **aman** karena feature itu justru mematikan `cfg(dev)`. **Jawaban auditor atas pertanyaan pokok "dibuktikan sintetis — terpenuhi?": sebagian, dan itu batas terjauh yang mungkin ditegakkan otomatis** — manifest membuktikan sebuah klaim sengaja ditulis dan terlihat di diff, bukan bahwa isinya sungguh sintetis; sisanya wajib ditutup review manusia atas teks `purpose`, dan itu perlu tertulis supaya tidak dikira otomatis penuh. |
| 2026-08-13 00:10 | SETUP-05 | `in-progress` | `in-progress` | **Tester LULUS (237 test, +34 miliknya); siklus 2 dibuka untuk menutup Critical auditor, bukan karena tester gagal.** **Yang paling berharga: enam skenario penolakan yang implementer jalankan lalu bersihkan kini permanen dan berjalan tiap `npm test`** — `tests/unit/check-fixture-content.e2e.test.js` menjalankan **CLI aslinya**, bukan reimplementasi fungsi murni, lewat git sungguhan di repo throwaway; keluaran `npm test` sekarang memuat dua belas baris `1 of 1 candidate(s) rejected` yang dapat dibaca siapa pun. Itu persis lubang yang sama dengan SETUP-04 dan kini tertutup di kedua item. **Jebakan #1 yang saya tandai terjawab dengan bukti, bukan penalaran:** index kosong sungguhan menghasilkan **exit 1** dengan pesan "nothing was verified", bukan hijau — jadi "tidak ada fixture" memang dibedakan dari "ada tapi tak terbaca". Dan skenario tak-terbaca dipaksa **nyata**: stage `.aero` + manifest benar, verifikasi baseline lolos, lalu **objek blob-nya dihapus dari `.git/objects/`** → guard gagal dengan "could not be confirmed present in the index". Tester juga menjalankan guard di luar repo git dan tanpa `git` di PATH: keduanya exit 1, fail-closed terkonfirmasi empiris. **Tester menemukan dua cacat di berkasnya sendiri dan memperbaikinya alih-alih melonggarkan assertion:** ia salah paham semantik `findFixturesRoot` — fungsi itu mencari segmen `fixtures` **topmost**, bukan yang terdekat ke berkas — sehingga 8 dari 16 test E2E gagal; diperbaiki dengan memberi tiap skenario root `fixtures/` sendiri, sesuai desain guard yang sebenarnya. **Satu observasi arsitektur dilaporkan apa adanya, bukan sebagai temuan:** `check-fixture-content.js` meresolusi `root` dari lokasi berkasnya sendiri (konsisten `check-dist-html.js`), sehingga **tidak ada cara** menjalankan CLI asli terhadap repo lain tanpa menyalin skripnya — itulah sebabnya E2E menyalin kedua skrip ke repo throwaway. **Satu false positive ditemukan dan sengaja tidak diperbaiki:** signature BMP hanya dua byte ASCII `"BM"`, jadi prosa apa pun yang diawali `BM` ikut disniff sebagai media. Arahnya fail-closed sehingga bukan lubang keamanan — ongkosnya satu entri manifest yang sebenarnya tidak perlu — dan tester mengunci perilakunya supaya terlihat alih-alih berubah diam-diam. **Ia juga mengunci ADR-0019 ke `package.json`**: `test:perf:shell`/`test:perf:core` dicocokkan **persis** ke string ADR-0019 termasuk `--features tauri/custom-protocol`, dengan alasan yang saya terima — edit diam-diam yang menghapus satu flag akan membuka kembali bug yang ADR-0019 tutup, dan nol test lain akan menyadarinya. **Angka yang diukur:** guard ~570–590 ms per jalan atas 64 path terindeks; berkas E2E ~93–181 s dengan varians tinggi murni dari overhead spawn proses di mesin ini (konsisten catatan `gitignore-guard.test.js`: 1,5–8,7 s per kasus); budget 90 s per test, terpakai terbanyak ~18 s. `npm test` penuh ~81 s. **Diverifikasi ulang coordinator:** `npm test` 237/237 · `npm run lint` + `postlint` bersih · `vue-tsc` exit 0 · `cargo fmt --all --check` exit 0 · `clippy --workspace --all-targets -D warnings` nol warning · `cargo test --workspace` 19/19. **Sisa untuk menutup siklus: C1, W2, W4, W3** — didelegasikan ke implementer. |
| 2026-08-13 01:20 | SETUP-05 | `in-progress` | `in-progress` | **Siklus 2 selesai: C1 ditutup dengan bukti sebelum/sesudah yang menohok; tester + auditor didelegasikan ulang.** **Bukti C1 adalah yang terkuat yang bisa diminta:** implementer membangun path index sungguhan `fixtures/fixtures.manifest.json\nzzz-tail-never-inspected.dat` menunjuk blob berisi teks lirik pujian, lalu menjalankan kedua versi guard. **SEBELUM: `1 candidate(s) verified synthetic, clean`, exit 0** — lirik itu duduk tak terdeklarasi di index dan guard melaporkan bersih. **SESUDAH: ditolak, exit 1.** Mekanismenya terbukti persis seperti dugaan auditor: karena prefiks sebelum `\n` sengaja disamakan dengan path nyata, korelasi bergeser dan guard lama membaca **isi manifest yang sah** sebagai konten path jahat itu — blob jahatnya sendiri tidak pernah dibaca sama sekali. **Dua premis brief saya salah dan implementer mengukur keduanya:** (1) "satu-satunya byte terlarang di path git adalah `/` dan NUL" **salah di Windows** — `core.protectNTFS`/`protectHFS` default `true` di sana dan menolak `\n` lewat `update-index`, sehingga reproduksi butuh `-c core.protectNTFS=false` dulu; tapi justru ini memperjelas bobotnya, karena **kedua proteksi itu default `false` di Linux/macOS**, yaitu tempat CI hidup. (2) "cat-file atas `:path` gitlink mengembalikan objek bertipe `commit`" **tidak terbukti** — diukur langsung, hasilnya **selalu `missing`**, karena commit object submodule hidup di `.git/modules/`, bukan di object database superproject. Konsekuensi kode lama yang ia temukan dari situ lebih buruk daripada dugaan W3: `identifyCandidates` melihat "missing, tanpa ekstensi terdaftar" lalu **diam-diam melewatkannya** — bukan kandidat, bukan pula `unreadable`. **W3 diperbaiki, bukan didokumentasikan sebagai batas sadar**, dan `--recurse-submodules` **ditolak dengan alasan yang benar**: flag itu hanya menyusuri submodule yang kebetulan sudah di-checkout di mesin yang menjalankan guard, sehingga hasilnya bergantung mesin — kelas kegagalan ADR-0020. Diganti penolakan unconditional atas tiap entri gitlink (mode `160000`) lewat `git ls-files -z -s`. **C1 ditegakkan di `classifyIndexedPaths`, bukan hanya di `buildBatchRequest`** — path tak-aman masuk bucket `unsafe` sebelum menyentuh allowlist maupun `toInspect`, lalu dilaporkan sebagai verdict bermakna alih-alih exception yang menghentikan seluruh run; `buildBatchRequest` tetap melempar sebagai lapisan kedua yang mendokumentasikan prasyaratnya. Doc `parseCatFileBatch` kini **menyebut invarian yang membuat korelasi posisi sah**, yang sebelumnya justru bagian yang hilang. Batas byte dipilih `\n` + `\r` dengan alasan tertulis, dan byte kontrol lain sengaja tidak disaring karena tidak satu pun memecah protokol ini — "pemeriksaan yang tak bisa dijelaskan setahun lagi". W2 dan W4 ditutup: komentar `sniffMediaKind` dibuat berdiri sendiri, dan test allowlist naik dari `toContain` ke `toEqual` sehingga pelebaran wajib terlihat di diff. **False positive BMP sengaja dibiarkan** — implementer setuju dengan penilaian tester bahwa arahnya fail-closed dan ongkosnya satu entri manifest. **Diverifikasi ulang coordinator:** `npm test` **249** (237 → 249, +12 test fungsi murni baru; **nol** berkas test tester tersentuh) · lint + `postlint` bersih · `vue-tsc` exit 0 · `cargo fmt --all --check` exit 0 · `clippy` nol warning · `cargo test --workspace` 19/19 · `test:perf:shell` dan `test:perf:core` exit 0. |
| 2026-08-13 01:50 | SETUP-05 | `in-progress` | `in-progress` | **Audit ulang siklus 2: 0 Critical — C1 terkonfirmasi tertutup — tetapi 2 Warning baru, keduanya kelas kegagalan yang sama yang item ini dibangun untuk mencegah.** Tester masih berjalan. **Penutupan C1 diverifikasi auditor di kode, bukan dari laporan:** `classifyIndexedPaths` dipanggil sebelum satu-satunya pemanggilan `buildBatchRequest`, dan bucket `unsafe` dicek **sebelum** allowlist, jadi path ber-`\n` tidak bisa menyelinap lewat allowlist; grep seluruh `scripts/` mengonfirmasi nol pemanggil produksi lain. Auditor juga memeriksa **sisi respons** seperti diminta: header objek dibangkitkan git dari metadata dan tak pernah memuat teks path, entri `missing` dibandingkan sebagai string literal penuh, dan pembacaan konten berbasis panjang byte bukan pemindaian delimiter — ia menyimpulkan klaim "byte kontrol lain tidak memecah protokol ini" **benar, bukan overclaim**. W2, W3, W4 tertutup; cakupan penolakan gitlink bahkan lebih luas dari yang diminta (seluruh index, bukan hanya `fixtures/`). **[W-new-1] diverifikasi ulang coordinator di `fixture-content-guard.js:453-458`:** bila konten tak terbaca **dan** path tidak berekstensi terdaftar, kode `continue` tanpa mencatat apa pun — bukan kandidat, bukan `unreadable`. Media biner yang hanya ada di stage konflik merge, atau yang hilang akibat race index berubah di tengah scan, **tidak pernah dibaca dan tidak pernah dilaporkan**. Ini persis rumusan C1 lewat rute berbeda: guard melapor bersih atas byte yang tak pernah dibacanya. Auditor jujur menyatakan gap ini **sudah ada sejak siklus 1** dan bukan diperkenalkan `-s`; peralihan ke `-s` hanya membuatnya terlihat. **[W-new-2] diverifikasi ulang coordinator di `fixture-content-guard.js:171`:** allowlist dievaluasi **tanpa syarat ekstensi maupun tipe konten**, sehingga `.aero` sungguhan berisi lirik jemaat yang ditaruh di `src-tauri/icons/service.aero` tidak pernah masuk `toInspect`, tidak pernah butuh entri manifest, dan `npm test` melaporkan bersih. Perilakunya terdokumentasi sengaja di komentar — jadi bukan overclaim ala W2 lama — tetapi konsekuensinya menabrak langsung tuntutan ADR-0023 "di mana pun". Belum pernah dilaporkan di siklus mana pun sebelumnya. **Keduanya murah ditutup dan keduanya akan ditutup, bukan ditunda:** `unreadable` harus dicatat terlepas dari ekstensi, dan allowlist seharusnya hanya membebaskan dari **sniffing media**, bukan dari pemeriksaan ekstensi terdaftar. **Suggestion:** verdict ganda saat merge conflict (bising, tak berbahaya); nol test E2E untuk penolakan gitlink dan skenario stage 1/2/3 — keduanya baru diuji sebagai fungsi murni; `.toString('utf8')` atas keluaran `ls-files -z -s` dapat memangling path non-UTF8 di Linux, konsisten di kedua arah sehingga bukan misatribusi, tetapi berinteraksi dengan W-new-1. **Batas auditor dinyatakan sendiri:** tanpa `Bash` ia tidak dapat membuktikan perilaku `ls-files -s` pada merge conflict nyata, jadi kesimpulannya dari semantik git terdokumentasi — ia meneruskan skenario konkretnya ke tester alih-alih membulatkannya. **W1 (`.aero` di-rename) bobotnya tidak berubah** oleh siklus 2: `hasDeclaredExtension` dan `sniffMediaKind` tidak disentuh sama sekali. |
| 2026-08-13 02:30 | SETUP-05 | `in-progress` | `in-progress` | **TERBLOKIR pada lingkungan, bukan pada pekerjaan. Working tree memuat 26 test yang belum pernah dieksekusi siapa pun; item tidak boleh ditutup.** **Tester menolak melaporkan LULUS dan itu keputusan yang benar:** Bash tool-nya gagal total sepanjang sesi — lebih dari sepuluh percobaan, termasuk `echo hello`, `git --version`, dan `whoami`, tidak satu pun selesai. Ia menulis 26 test (6 E2E di `check-fixture-content.e2e.test.js`, 20 di `fixture-content-guard.extra2.test.js` yang baru) lalu menyatakan terang-terangan bahwa nol di antaranya pernah dikompilasi apalagi dijalankan, dan **secara eksplisit merekomendasikan item ini tidak ditutup atas dasar laporannya**. Ia menamai sendiri tiga risiko konkret yang hanya terlihat saat dieksekusi: apakah `git submodule add -c protocol.file.allow=always` jalan di mesin Windows ini, apakah `-c core.protectNTFS=false` cukup untuk `update-index --cacheinfo` menerima path ber-`\n` di versi git mesin ini, dan apakah teks stderr yang ia `toMatch` — disalin dari pembacaan sumber, bukan dari keluaran nyata — benar-benar cocok. **Verifikasi coordinator juga gagal, dan dengan gejala yang sama:** `npm test` melampaui batas **10 menit** lalu dibunuh (baseline sebelumnya ~81 detik), dan sesudahnya bahkan `echo alive` tidak selesai dalam 60 detik. Jadi ini bukan cacat berkas test tester melainkan degradasi lingkungan yang menimpa coordinator dan agent sama saja. **Nol commit dibuat**, karena nol perintah git dapat dijalankan untuk memverifikasi apa yang akan di-commit. **Yang sudah tervalidasi dan tidak perlu diulang** (dijalankan coordinator sebelum degradasi): siklus 2 hijau di keenam gate dengan `npm test` 249/249. Yang belum: 26 test tester siklus 2, dan kedua Warning auditor. **Sisa pekerjaan yang sudah terdefinisi penuh dan tinggal dieksekusi:** (1) jalankan 26 test itu dan perbaiki kesalahan sintaksis/asersi apa pun; (2) tutup W-new-1 — `unreadable` wajib dicatat terlepas dari ekstensi; (3) tutup W-new-2 — allowlist hanya membebaskan dari sniffing media, bukan dari pemeriksaan ekstensi terdaftar; (4) siklus 3: pembangkitan tipe Rust→TS (NFR-33). **Catatan silang yang menguatkan keduanya:** tester menemukan celah W-new-1 secara **independen** dari auditor dan mendokumentasikannya di berkas testnya sebagai celah yang sengaja tidak ia tutup — dua agent yang tidak saling melihat sampai pada temuan yang sama. |
| 2026-08-13 10:25 | SETUP-05 | `in-progress` | `in-progress` | **Lingkungan pulih; 26 test tester akhirnya dieksekusi coordinator. Hasil: 258 lulus, 1 gagal, 13 skip — dan skip-nya adalah temuan, bukan catatan kaki.** **Bagian terpenting siklus 2 terbukti di git sungguhan:** skenario misattribution C1 ditolak, dan yang paling tajam — path ber-`\n` yang diletakkan **di bawah prefix allowlist** `src-tauri/icons/evil\nsmuggled.ico` tetap ditolak, sehingga precedence `unsafe` di atas allowlist terbukti *end-to-end* lewat CLI asli, bukan hanya sebagai fungsi murni. Klaim implementer bahwa `-c core.protectNTFS=false` cukup untuk menyisipkan path ber-`\n` juga terkonfirmasi di mesin ini. Sembilan belas test fungsi murni tester lulus percobaan pertama (ia melaporkan 20 `it()`; yang benar-benar berjalan **19**). **Kegagalan 1 — suite gitlink tidak pernah berjalan:** `beforeAll` timeout di **10.000 ms**, karena vitest memisahkan `hookTimeout` dari `testTimeout` dan tester menetapkan budget 90 s **per test** saja. Akibatnya penolakan submodule — satu-satunya perbaikan siklus 2 yang belum terbukti — masih **belum** terverifikasi end-to-end. **Kegagalan 2 lebih berbicara:** `setup05-lifecycle.test.js:102` timeout di **5.000 ms default** saat memanggil `git ls-files`. Test itu **lulus di run sebelumnya** — bukan karena benar, melainkan karena mesin sedang cepat. Pelajaran SETUP-06 terulang verbatim: lulus tanpa margin. **Yang mengubah bobot keduanya:** nol `skipIf`/`it.skip` di seluruh berkas test (di-grep), jadi **ketiga belas skip itu seluruhnya akibat `beforeAll` yang timeout**. Bandingkan dua run coordinator: E2E dijalankan sendirian → **2 skip**; di dalam suite penuh → **13 skip**. Berkas E2E punya empat `beforeAll`, dan berapa yang selamat **bergantung beban mesin** — jadi lulus/gagalnya tidak deterministik, persis kelas ADR-0020 yang item ini habiskan banyak siklus untuk usir. **Diagnosis coordinator atas meltdown 02:30 dikoreksi sendiri:** dugaan "proses git/node yatim" **salah** — `tasklist` pengguna menunjukkan nol `git.exe` dan dua `node.exe` dengan working set praktis nol. Penyebab yang jauh lebih mungkin: tester + auditor dijalankan **paralel**, keduanya men-spawn git bertubi-tubi, dan berkas E2E sendirian saja makan **189 detik**. **Kebijakan baru: agent yang menjalankan suite git-berat diserialkan, tidak lagi paralel.** **Urutan sisa pekerjaan ditetapkan:** implementer menutup W-new-1 + W-new-2 di berkas guard lebih dulu (berkas berbeda dari milik tester, tapi perilaku barunya menentukan asersi tester), lalu tester memperbaiki kedua budget waktu **dan** menutupi perilaku baru — terutama `.aero` di bawah `src-tauri/icons/`, yang hari ini nol test menyentuhnya. |
| 2026-08-13 12:42 | SETUP-05 | `in-progress` | `in-progress` | **Implementer siklus 3 mati oleh limit sesi (reset 12:40) di tahap survei, sebelum menulis satu berkas pun; didelegasikan ulang dua menit sesudah reset.** Keluaran terakhirnya masih berupa rencana membaca `setup05-lifecycle.test.js` sebagai konteks dan mengonfirmasi nol pemanggil produksi lain atas `classifyIndexedPaths`/`identifyCandidates`. **Working tree diverifikasi coordinator: nol residu**, sebelas entri yang sama persis. **Tidak dihitung sebagai kegagalan H3** — kematian agent kedelapan di sesi ini, diperlakukan sama seperti tujuh sebelumnya. Brief siklus 3 disimpan utuh; nol perubahan diperlukan karena agent belum sempat membantah premis apa pun. |
| 2026-08-13 13:45 | SETUP-05 | `in-progress` | `in-progress` | **Siklus 3 selesai: W-new-1 dan W-new-2 ditutup dengan bukti sebelum/sesudah. Satu test tester gagal, dan kegagalannya adalah hasil yang benar.** **W-new-1** — `identifyCandidates` kini mendorong **setiap** path yang kontennya tak terkonfirmasi ke `unreadable`, terlepas dari ekstensi. **Implementer menolak setengah dari arah yang saya sodorkan, dengan alasan yang saya terima:** ia memasukkannya ke `unreadable` saja dan **bukan** ke `candidates`, karena `candidates` berarti klaim "terbukti media atau berekstensi terdaftar" — sedangkan untuk path yang byte-nya gagal dibaca, guard **tidak punya byte untuk disniff**, sehingga menyebutnya kandidat adalah overclaim, jenis yang sama yang ADR-0020/0021 tunjukkan mahal. Yang benar dilaporkan adalah "tidak bisa dipastikan", dan `summariseRun` sudah menggagalkan run pada `unreadableCount > 0` secara independen. **W-new-2** — allowlist kini mempersempit **apa** yang diperiksa, bukan **apakah**: ia hanya membebaskan dari round-trip `cat-file`/sniffing konten, yang memang tujuan aslinya, sementara `hasDeclaredExtension` — murni string match, nol I/O — dijalankan atas bucket `allowlisted` lewat fungsi baru `identifyAllowlistedExtensionCandidates`. `classifyIndexedPaths` **tidak disentuh strukturnya**, sehingga precedence `unsafe` di atas allowlist yang sudah terbukti end-to-end tetap utuh. **Bukti keduanya nyata, lewat CLI asli di repo git sungguhan:** `src-tauri/icons/service.aero` berisi lirik — SEBELUM `0 candidate(s) found ... Nothing to declare`, exit 0; SESUDAH ditolak dengan pesan yang tepat, exit 1. Berkas ber-header PNG berekstensi `.dat` yang blob-nya dihapus dari `.git/objects/` — SEBELUM exit 0, SESUDAH `could not be verified`, exit 1. **Kegagalan test yang benar:** `scripts/fixture-content-guard.extra2.test.js`, test bernama *"is a real, named gap for an UNDECLARED (non-extension) path in the same missing-content shape — documented, not fixed"*, mengasersi `unreadable` bernilai `[]`. Tester **sengaja mengunci perilaku lama sebagai celah yang ia dokumentasikan tapi tidak tutup** — dan ia menemukan celah itu independen dari auditor. Test itu berfungsi sebagai kenari: ia berbunyi tepat saat celahnya ditutup. Implementer melaporkannya dengan nama dan path alih-alih menyuntingnya, sesuai batas keras. **Gate:** `cargo fmt` exit 0 · `clippy` nol warning · `cargo test --workspace` 19/19 · `vue-tsc` exit 0 · `npm test` 275 lulus, 1 gagal (kenari di atas), 3 skip. **`postlint` MERAH dan diverifikasi coordinator** — `prettier --check` menolak `scripts/fixture-content-guard.extra2.test.js` dan `tests/unit/check-fixture-content.e2e.test.js`; keduanya pra-eksisting milik tester, tidak disentuh implementer, dan implementer sengaja **tidak** menjalankan `--write` atasnya karena bukan berkasnya. **Data baru soal flakiness:** pada run ini `setup05-lifecycle.test.js` justru **lulus penuh** sementara **dua** `beforeAll` di berkas E2E kena timeout (sebelumnya satu). Itu mengonfirmasi langsung bahwa kedua berkas ini lulus-gagalnya bergantung beban, bukan logika. |
| 2026-08-13 22:50 | SETUP-05 | `in-progress` | `in-progress` | **Ronde penutup tester: budget hook berhasil, nol skip untuk pertama kalinya, dan test gitlink akhirnya benar-benar dieksekusi — lalu gagal, tapi gagal pada cara menghitung, bukan pada yang dihitung.** **Suite penuh: 284 test, 283 lulus, 1 gagal, 0 skip** (baseline sebelumnya 275 lulus, 1 gagal, 3 skip). **Angka terpenting bukan itu, melainkan 189 s → 20,7 s pada `check-fixture-content.e2e.test.js`, dan 20 test (2 skip) → 22 test (0 skip).** Keempat `beforeAll` selamat. Ini menutup temuan lama bahwa seluruh skip di repo berasal dari hook timeout dan bukan dari `skipIf` — nol `skipIf` memang ada di mana pun. Kontrol `tests/unit/setup05-hook-timeout-control.test.js` menahan klaim itu supaya "semua lulus" tidak bisa lagi berarti "budget-nya yang tidak berfungsi", mengikuti preseden `--testTimeout=1` dari SETUP-06. **Konsekuensinya: describe gitlink (auditor W3, siklus 2) berjalan end-to-end untuk pertama kalinya sejak ditulis** — sebelumnya ia selalu mati di hook, jadi penolakan gitlink tidak pernah sekalipun terbukti lewat CLI asli. Premisnya ikut terukur: `git cat-file --batch` atas path gitlink menjawab `missing`, bukan objek `commit`, persis seperti yang didokumentasikan `gitlinkVerdict`. **Kegagalan tunggal: `expected 4 to be 2`** pada `result.stderr.match(/git submodule/g)`. Sebabnya diagnostik coordinator, bukan cacat guard: string alasan di `scripts/fixture-content-guard.js:756,758` menyebut frasa itu **dua kali per penolakan** ("this is a git submodule (mode 160000)" dan "AeroWorship does not use git submodules"), jadi dua penolakan sah menghasilkan empat sebutan. **Tapi asersi itu menjaga sesuatu yang nyata dan tidak boleh dilemahkan begitu saja:** komentarnya sendiri menyebut bahayanya — gitlink di luar `fixtures/` bisa saja ditolak oleh alasan "tak punya leluhur fixtures/" dan bukan karena ia gitlink, sehingga cek gitlink yang rusak tersamarkan. Didelegasikan ke tester untuk **diukur per-path**, bukan sekadar diperbaiki angkanya. |
| 2026-08-13 23:05 | SETUP-05 | `in-progress` | `in-progress` | **Separuh guard SETUP-05 selesai: 284 test, 0 gagal, 0 skip, lint + typecheck hijau. Asersi gitlink terakhir ternyata tidak pernah menguji apa pun, dan itu terbukti dengan pengukuran, bukan dengan penalaran.** Diagnosis coordinator atas `expected 4 to be 2` benar — alasan gitlink menyebut frasa "git submodule" dua kali per penolakan (`fixture-content-guard.js:756,758`), jadi dua penolakan sah menghasilkan empat sebutan. **Tetapi premis coordinator tentang bahaya yang dijaga asersi itu SALAH, dan tester membantahnya dengan mengukur.** Dugaan saya: gitlink di luar `fixtures/` ditolak oleh alasan "tak punya leluhur fixtures/" sehingga menyamarkan cek gitlink yang rusak. Kenyataan terukur dengan `isGitlinkMode` dilumpuhkan: gitlink **tidak pernah menjadi candidate sama sekali** — objek commit-nya `missing` di superproject, jadi `evaluateCandidate` tak pernah dipanggil — dan keduanya jatuh ke bucket `unreadable`. **Konsekuensinya lebih buruk daripada yang saya duga: dengan cek gitlink mati total, kedua path tetap tercetak dan exit tetap 1, sehingga `toContain` dan `status).not.toBe(0)` tetap hijau.** Asersi lama itu hijau terhadap guard yang rusak — kelas cacat yang sama persis yang [ADR-0020](decisions.md#adr-0020)/[ADR-0021](decisions.md#adr-0021) buktikan mahal, ditemukan di dalam test yang ditulis untuk memberantasnya. **Asersi baru mengukur per-path** lewat bentuk keluaran sungguhan (`check-fixture-content.js:204` mencetak header `fixture content guard: <path>` lalu alasan berindentasi 4 spasi; paragraf ringkasan berbagi prefiks tapi tak punya baris berindentasi, dan indentasi itulah pembedanya): kunci map harus persis kedua path, tiap path tepat satu alasan, ter-anchor `^this is a git submodule \(mode 160000\)`, plus `2 of 2 candidate(s) rejected.` untuk memastikan itu dua *violation* dan bukan dua entri gagal-baca. **Bukti negatif merah→hijau dijalankan lewat salinan sekali-pakai guard di `%TEMP%`** — `scripts/` tidak disentuh, working tree bersih sesudahnya. **Angka yang menutup temuan lama:** `check-fixture-content.e2e.test.js` 189 s → 20,7 s, 20 test (2 skip) → 22 test (0 skip); nol skip di seluruh repo untuk pertama kalinya, mengonfirmasi bahwa seluruh skip memang berasal dari hook timeout dan bukan dari `skipIf` mana pun. Kontrol `tests/unit/setup05-hook-timeout-control.test.js` menahan klaim itu, meniru preseden `--testTimeout=1` SETUP-06. **Keempat temuan yang ditunda kini punya alamat permanen: [ADR-0030](decisions.md#adr-0030) (W1), [ADR-0031](decisions.md#adr-0031) (S1), [ADR-0032](decisions.md#adr-0032) (S2), [ADR-0033](decisions.md#adr-0033) (S3).** Menuliskannya mengubah dua di antaranya: W1 bukan lubang yang belum ditambal melainkan **tak dapat ditambal oleh magic byte sama sekali**, karena FR-701/Appendix C mendefinisikan `.aero` sebagai JSON UTF-8 polos yang tak punya tanda tangan — beda sifat dari celah `.pptx`/`.pdf` yang punya signature dan hanya menunggu item yang membutuhkannya; dan S3 terbelah dua sebab, di mana path non-UTF-8 ternyata **gagal-tertutup** berkat perbaikan W-new-1 siklus 3 sehingga yang kurang adalah kualitas diagnostik, bukan kekedapan. **Belum ditutup:** separuh NFR-33 (pembangkitan tipe Rust→TS) belum tersentuh, dan perubahan kode produksi siklus 3 (W-new-1, W-new-2) belum diaudit. Auditor dan implementer dijalankan paralel — aman karena `security-auditor` read-only tanpa Bash, jadi kebijakan serialisasi agent berat-git tidak dilanggar. |
| 2026-08-13 23:40 | SETUP-05 | `in-progress` | `in-progress` | **Audit siklus 3: 0 Critical, 4 Warning, 4 Suggestion — dan tiga dari empat Warning menembak ADR yang baru saya tulis sendiri, persis seperti yang saya minta.** **Pertanyaan pokok terjawab bersih:** perbaikan W-new-1 tidak membuka jalur gagal-terbuka. Auditor menelusuri keenam titik kembali `main()` satu per satu — baris 131, 145, 163, 169, 231, 244 — dan membuktikan 244 hanya tercapai bila `summary.exitCode === 0`, yang menuntut `unreadableCount === 0`; tak ada kombinasi `violations` kosong + `unreadable` terisi yang berujung laporan bersih. W-new-2 simetris dua arah dan dibuktikan dari kode: `.aero` di bawah `src-tauri/icons/` ditolak tanpa syarat lewat `findFixturesRoot` yang mengembalikan `null`, sedangkan kelima ikon nyata gagal di `hasDeclaredExtension` dan tak pernah masuk `candidates`. Hitung ganda mustahil sebab `classifyIndexedPaths` menaruh tiap path di **tepat satu** bucket. Precedence bertahan, bahkan lebih kuat dari yang diminta: gitlink disaring **sebelum** `classifyIndexedPaths`, jadi ia mendahului `unsafe` juga. **W1 — cacat kode produksi sungguhan, dan ia membatalkan klaim [ADR-0033](decisions.md#adr-0033).** `parseCatFileBatch` mendekode header sebagai `latin1` (benar, demi melindungi framing biner) lalu membandingkannya dengan `` `:${key} missing` `` yang ber-UTF-8. Path non-ASCII → gema byte-mentah git jadi mojibake → tak pernah `===` → jatuh ke regex header yang juga tak cocok → **melempar**, dan pemanggilnya `return 1` **sebelum `identifyCandidates` berjalan untuk satu path pun**. Jadi yang kurang bukan kualitas diagnostik seperti yang saya tulis, melainkan **cakupan**: satu path bermasalah menggugurkan penilaian atas semua path lain. Tetap gagal-tertutup, jadi bukan celah — tapi mekanisme yang saya kreditkan tidak pernah menyala. **W3 — [ADR-0030](decisions.md#adr-0030) runtuh seluruhnya, dan saya verifikasi sendiri sebelum menerimanya.** `docs/PRD.md:1467` dan `:1526` sama-sama mewajibkan `"kind": "aeroworship.session"` — sentinel ASCII literal, jadi klaim saya "tidak ada yang bisa ditambahkan ke daftar" salah. Appendix C lengkap di PRD `:1460–1549`; yang belum ada implementasi Rust-nya, bukan skemanya. Argumen biaya juga salah: `check-fixture-content.js:72–74` menyatakan sendiri guard **sudah** membaca isi lengkap setiap berkas ter-track non-allowlist, jadi biaya tambahannya nol. **Tetapi arah perbaikan yang auditor sarankan sendiri lebih lebar daripada kenyataan, dan saya menangkapnya:** `docs/PRD.md` ter-track dan memuat literal itu dua kali, sehingga `buffer.includes(...)` yang polos akan menolak PRD sendiri dan membuat `npm test` merah pada repo bersih. Deteksinya wajib menuntut **JSON yang sah** ber-`kind` tersebut. **W4 — [ADR-0031](decisions.md#adr-0031) mengklaim verifikasi yang tidak ada:** arah manifest → berkas tidak pernah diperiksa, entri yatim lolos senyap. Dua klausul lain benar, dan satu lebih kuat dari yang saya tulis (manifest wajib ada di **index**, bukan working tree). **W2 — [ADR-0032](decisions.md#adr-0032) salah premis:** yang mengalir lewat `--batch` adalah **seluruh isi repo ter-track** dikurangi allowlist, bukan hanya kandidat, sehingga pemicu pembatal yang saya tulis ("fixture media besar pertama") tak akan pernah menyala untuk penyebab yang paling mungkin. Diukur ulang: **64 berkas, total 0,8 MiB, margin ~80×** terhadap 64 MiB. Klaim gagal-tertutupnya sendiri terverifikasi. **Keempat ADR ditulis ulang total** dengan koreksinya dinyatakan terbuka, bukan disunting diam-diam; `prettier --check decisions.md` hijau. **Konsekuensi H2: keempat Warning belum tuntas.** W2/W3/W4 ditutup lewat penulisan ulang ADR; W1 adalah kode produksi dan **diperbaiki, tidak ditunda**, bersama deteksi sentinel ADR-0030 dan tiga Suggestion (S1 verdict yang faktual salah untuk kandidat allowlist di bawah `fixtures/`; S2 entri konflik merge memblokir `pretest` dengan sebab yang salah; S3 dua parameter `summariseRun` yang tak pernah dipakai). Itu menjadi siklus 4, ditahan sampai implementer NFR-33 selesai — kebijakan serialisasi agent berat-git. |
| 2026-08-14 00:15 | SETUP-05 | `in-progress` | `in-progress` | **Separuh NFR-33 selesai: kontrak Rust→TS lahir beserta gate anti-drift-nya, dan janji "build time" PRD terbukti tidak dapat ditepati oleh alat yang PRD sendiri namai.** Seluruh gate hijau: `cargo fmt`, `clippy -D warnings`, `cargo test --workspace` 20 lulus (naik dari 19), `npm run lint`, `npm run typecheck`, `npm test` 284 lulus 0 gagal 0 skip — angka tester tidak bergeser. **Pertentangan PRD, diverifikasi coordinator di sumber crate bukan diterima dari laporan:** `ts-rs-12.0.1/src/lib.rs:168` berbunyi *"bindings \_\_cannot\_\_ be exported during compile time"*, sebab makro prosedural dievaluasi sebelum tahap kompilasi lain; `#[ts(export)]` mengembang jadi `#[test]`. PRD §6.4 (baris 585) menjanjikan build time, PRD §6.12 (baris 814) menamai `ts-rs` lebih dulu, dan keduanya tidak dapat berlaku bersamaan. **`docs/PRD.md` tidak disentuh** (H4) — dicatat di [ADR-0034](decisions.md#adr-0034) dan **diajukan ke pengguna sebagai keputusan**. NFR-16 diverifikasi coordinator: `cargo tree -p aeroworship --edges normal` memberi **nol** ts-rs; ketiga crate baru dev-only, nol byte masuk installer. **Gate anti-drift dibuktikan merah lalu hijau** dengan mengubah `name: &'static str` → `Option<&'static str>` tanpa meregenerasi: `bindings:check` exit 1 dan menunjuk baris 16 (`name: string | null` vs `name: string`), `typecheck` ikut merah lewat hook. Coordinator menjalankan sendiri gate-nya: hijau, dan `git status` sesudahnya membuktikan regenerasi memang masuk temp dir — gate tidak diam-diam memperbaiki drift yang seharusnya ia laporkan. Dua sifat diukur bukan diasumsikan, keduanya di [ADR-0035](decisions.md#adr-0035): `cargo test <filter>` yang tak cocok apa pun **exit 0**, sehingga "generator menghasilkan nol berkas" dijadikan kegagalan eksplisit — tanpa klausa itu, menghapus `#[ts(export)]` terakhir mematikan seluruh mekanisme dengan gate tetap hijau, persis kelas [ADR-0020](decisions.md#adr-0020). **Dua harga diterima terang-terangan:** `npm run typecheck` kini menuntut toolchain Rust, dan `cargo test --workspace` menulis ke berkas ter-track sehingga drift muncul sebagai berkas termodifikasi di `git status` alih-alih gate merah — backstop untuk commit selektif adalah tinjauan manusia, sebab **repo ini belum punya konfigurasi CI sama sekali**. [ADR-0036](decisions.md#adr-0036) mengunci bahwa `Migration` adalah bukti mekanisme dan **bukan** awal katalog Appendix D, lengkap dengan instruksi pembongkarannya saat tipe kawat pertama mendarat; `AppError` sengaja tidak diciptakan karena itu kontrak milik item FR. **Temuan implementer yang tidak saya sebut dan ternyata penting:** `src/shared/bindings/` sudah lebih dulu ada di `ignores` `eslint.config.js` dan `.prettierignore` sementara `tsconfig.json` tetap meng-include-nya — jadi nol konfigurasi lint perlu disentuh dan `vue-tsc` tetap benar-benar mengecek berkas hasil generate; dan `.gitattributes` (`* text=auto eol=lf`) menutup jebakan CRLF yang seharusnya membuat gate ini merah permanen di mesin ber-`core.autocrlf=true`, termasuk mesin ini. Item sebelumnya sudah menyiapkan jalannya. **Belum tuntas, dan implementer menamainya sendiri:** nol test untuk `scripts/bindings-guard.js`, sehingga keputusan gate tertutup hanya oleh bukti manual merah/hijau — bukan oleh gate. Itu kelas ADR-0020 yang ia tinggalkan terbuka dan milik tester. **Siklus 4 didelegasikan:** W1 (bandingkan sentinel `missing` level byte), deteksi sentinel Appendix C [ADR-0030](decisions.md#adr-0030) lewat JSON yang sah bukan substring, plus S1/S2/S3. |
| 2026-08-14 00:25 | SETUP-05 | `in-progress` | `in-progress` | **Implementer siklus 4 mati oleh limit sesi (reset 03:00) di tahap survei, sebelum menulis satu berkas pun.** Keluaran terakhirnya masih berupa rencana memeriksa [ADR-0030](decisions.md#adr-0030) dan mengukur premis P0 secara empiris. **Working tree diverifikasi coordinator: nol residu** — `scripts/fixture-content-guard.js` masih 12:52 dan `scripts/check-fixture-content.js` masih 13:04, keduanya tak tersentuh; sebelas entri `git status` identik dengan sebelum delegasi. **Tidak dihitung sebagai kegagalan H3** — kematian agent kesembilan di sesi ini, diperlakukan sama seperti delapan sebelumnya: batas lingkungan, bukan kegagalan pekerjaan. Delegasi ulang ditahan sampai limit pulih; mendelegasikan sekarang hanya akan mengulang kematian yang sama. **Yang tersisa sebelum SETUP-05 dapat ditutup, seluruhnya sudah berbentuk dan tidak menunggu keputusan apa pun kecuali satu:** (1) siklus 4 implementer — W1 sentinel `missing` lintas-pengodean, deteksi sentinel Appendix C lewat JSON yang sah, S1/S2/S3; (2) tester — nol test untuk `scripts/bindings-guard.js`, kelas [ADR-0020](decisions.md#adr-0020) yang implementer NFR-33 namai sendiri sebagai terbuka, plus verifikasi ulang sesudah siklus 4; (3) audit penutup atas permukaan binding yang belum pernah diaudit sama sekali; (4) **keputusan pengguna** atas pertentangan PRD §6.4 vs §6.12 yang tercatat di [ADR-0034](decisions.md#adr-0034). **Belum ada satu pun commit untuk SETUP-05** — working tree memuat empat siklus guard, seluruh berkas tester, dan seluruh pekerjaan NFR-33. |
